# NewPower：M0/M1 之后的持续产品研发目标

适用状态：已完成本机 M0/M1，继续在本地 `newpower` 开发和提交，不 push。本文用于替换旧启动指令中“本轮只完成 M0＋M1”的任务终点，保留其他有效工程约束。

依据：用户提供的 `acceptance.md`。其中记录 M1 功能提交 `463c1ab`，并叙述随后单位与布局修复；接手时以实际 HEAD、提交历史与工作区核对最终状态。该记录报告迁移 0012、绑定持久化、二维三维联动、异常恢复和权限等验证通过。本次只审核了验收文字，没有访问用户电脑上的源码、日志、截图或重新执行测试。

**对后续里程碑的判断**

原 M2–M8 的能力范围可以保留，执行顺序按依赖调整为：M2 → M3 → M4 → M6a → M5 → M6b → M7 → M8。运行器的基本契约、性能基线和 AI 所需编辑操作接口提前建立；M6a 的必要部分可随前面阶段推进。完整数字孪生引擎仍是产品目标。

| 阶段 | 调整后的交付重点 |
|---|---|
| M2 | 项目定义、稳定 ID、版本迁移、编辑操作、撤销重做、多页面；编辑与运行使用一致的项目解释规则 |
| M3 | 可复用三维场景、多模型和实例、资源版本与引用、模型更换与绑定修复 |
| M4 | 共用事件、条件、动作和状态机制，动画与二维三维交互编排、调试与复用 |
| M6a | 独立运行所需的数据连接、采集生命周期、凭据与环境配置、失败恢复；集中采集和多客户端共享 |
| M5 | 独立运行器、项目与资源导入导出、版本发布与回滚、本地交付包、内网运行适配 |
| M6b | 历史数据、告警和诊断增强；完善可扩展数据源适配及其产品界面 |
| M7 | 代表性规模测试与性能优化、长时间稳定性、专业场景能力；早期已经建立的性能基线在此扩大验证 |
| M8 | AI 通过统一编辑操作生成和修改项目，支持差异、校验、修复、撤销和保存，以真实任务评价 |

在目标输入框中复制下面整段；也可以把本文放进项目目录，让主研发会话读取并执行。附件中的验收文件及仓库现有状态记录应保留。

**可直接使用的完整目标提示词**

```text
持续推进 factory-digital-twin-platform 的 NewPower 数字孪生引擎，直至完成下述产品能力和总体验收。完整引擎是本次目标，M2–M8 是执行检查点；完成单个里程碑后继续下一项，不把里程碑结束当作整个目标完成。

一、沿用已完成成果

M0、M1 已有 acceptance.md 验收记录。先读取该文件、当前研发状态和适用项目规则，核对本地 newpower 的真实 HEAD、未提交内容、提交历史与现有服务。报告中的 463c1ab 是已知功能提交，之后还有单位与布局修复；确认最终修复已保存，不机械退回该提交。

这是未推送的本地研发成果，沿用当前仓库，不重新克隆后丢下本地进展，不将代码重置到远端旧版本。当前已有 0012_component_data_bindings.sql、pnpm test:newpower 和 Playwright 验收；命令与迁移以实际源码为准，不照旧启动手册重建环境。

复用已经运行的正确服务和隔离测试库。仅在状态变化、证据不足或新改动可能影响的范围复验必要基线，随后进入 M2；不重新实施 M0/M1，不重复整仓调研。已通过的记录保留，新的验证注明实际提交与环境。

二、产品范围与工作授权

newpower 交给我持续推进，同事主要在其他分支。允许你完成相关本地代码和文档编辑、依赖准备、本地迁移、服务启动、浏览器检查、测试、修复、必要结构优化和本地 commit。

只在本地 newpower 工作，不 push 到任何远端，不创建远端 PR，不修改或自动合并其他分支，不部署外部生产环境或修改远端数据库。产品的发布、部署适配和打包功能可以开发并在本地验证。研发所需公开资料及云浏览器访问已授权，不重复申请同类访问；遵循实际工具权限，不绕过限制。

由一个主研发会话统筹。可委派独立调查或只读复核，避免多个任务同时修改同一文件。普通技术选择、必要依赖、局部重构和修复自行判断并推进。旧任务中“本轮止于 M0＋M1”的范围已由本目标替代，更新 newpower 的适用规则和状态记录，保留其他有效约束。

三、按依赖持续实现

M2：建立可演进的项目定义、稳定实体 ID、schema 版本与迁移、编辑命令和撤销重做、多选分组复制、多页面、保存与恢复。保留 M1 独立绑定存储的价值，明确画布、绑定、资产、页面的引用和版本一致性。编辑预览与独立运行共用同一套解析和校验规则。先以“复制绑定组件→删除→撤销/重做→保存重开→预览仍正确”形成可操作交付，再覆盖跨页面引用与旧项目迁移。操作接口从此支持后续 AI 调用。

M3：完善可复用三维场景、多模型与实例、场景树、变换、材质灯光和镜头、资源引用与版本。资产导入失败要可定位；模型替换后能识别和修复失效绑定；保存重开、实例独立性及资源释放有验证。复用已有三维编辑能力，不另做互不相通的场景系统。

M4：实现统一的事件、条件、动作和状态执行，支持二维三维选择联动、动画、镜头或路径、交互编排、调试与复用。承接 M1 选择和数据状态，明确循环触发、异步动作、失败和取消的行为，保证编辑预览与运行一致。

M6a：把独立交付所需的数据连接与采集基础提前完成，包括连接和订阅生命周期、超时重试、并发与采样控制、鉴权和环境配置、断连恢复、集中采集及多客户端共享。区分已有的页面内请求复用与服务端采集能力。必要部分可以随 M2–M4 实现，不能在发布打包后才发现运行环境无法承接数据链路。

M5：完成独立运行器、项目和资源的导入导出、依赖检查、版本发布与回滚、本地可交付运行包及内网部署适配。在独立的干净本地环境验证，不依赖开发目录、编辑器进程或个人绝对路径。声明实际服务依赖并提供配置方式，凭据不固化进项目包；修改草稿不影响已发布版本。此阶段只验证发布功能，不推送或发布现有远端系统。

M6b：完善历史数据、告警记录与查询、运行诊断、数据转换和适配器扩展。以统一契约覆盖 REST、实时订阅、设备消息和只读数据库等数据来源，按当前实现合理组织 WebSocket、MQTT、SQL 适配。接口、配置界面、异常路径与本地可复现的集成测试都要完成；公司真实接口另有环境要求的，单独记录待验证项。

M7：完成代表性模型、设备和组件规模下的性能及长时间稳定性验证，按测量结果优化压缩、LOD、实例化、加载、内存与资源释放；继续完善 GIS 等已纳入产品范围的专业能力。性能观测从 M2 开始，M7 扩大负载而不是首次量测。明确硬件、浏览器、数据规模、预算和结果，不用任意数字声称达到工业规模。

M8：完成 AI 辅助创建和修改项目。AI 与人工共用受校验的编辑操作，支持差异展示、引用与权限校验、修复、撤销、保存和运行验证；用固定搭建任务评价效果。真实模型调用与模拟契约测试分别记录。研发阶段从现在起充分使用 Astra，产品 AI 入口到此具备完整可验收行为。

先把上述范围拆成简洁的能力清单、依赖与可执行验收条件，复用现有路线和状态文件，随后立即实施。默认按 M2→M3→M4→M6a→M5→M6b→M7→M8 推进；源码证明有更合适的依赖顺序时可调整并记录原因。不要为追求编号顺序阻止必要工作，也不要自行删除能力以宣布完成。

四、每个检查点的工作循环

选择下一项依赖已具备的具体能力，简述目标和验收方式，然后完成实现、界面操作、持久化、运行行为、相关测试、浏览器验证、修复与本地提交。同步优化本轮触及的旧实现，保持旧模板、旧项目、旧绑定和已有权限兼容。

每完成一项，更新实际证据和当前任务，创建清楚的本地提交，简要报告进展，然后自动进入下一项。不要在写完文档、通过构建、做完一个演示或完成某个 M 编号后停下来等待“继续”。不堆积未经验证的大批改动，也不为无关小修改反复运行全套检查。

维护当前任务、完成证据、已知问题、下一项操作和必要的设计决策。上下文压缩后按记录继续；不要凭记忆重做或丢弃当前工作。

五、总体验收

在开始大规模扩展前，把各能力的完成条件及代表性验证规模写清楚，后续变更须有理由，不能为绕过失败而降低标准。

目标完成至少需要：全部已约定核心能力清单通过；可以通过编辑器从空项目搭建可复用的二维三维项目，配置多资产数据和交互并保存重开；可导出到干净运行环境，更新版本和回滚；旧项目迁移与核心回归通过；错误、权限、连接恢复、资源生命周期和代表性性能有证据；AI 修改可校验、可撤销并通过真实任务验证。样例应调用通用机制，不能靠硬编码替代产品功能。

EasyV、EasyTwin 作为能力和体验对标。用明确场景、功能覆盖和测量记录判断进展；完整引擎目标不缩减为单个工厂演示。真实公司模型、工业接口和目标硬件尚不可用时继续本地可完成部分，并准确标明验证边界，不能据模拟结果宣称全面超越或生产验证通过。

只有总体验收完成、我明确要求暂停，或确有使其余工作无法继续的外部阻断／执行限制时，才结束本次执行。缺少材料时先继续其他可完成工作；普通失败先定位、修复、复验。确需我的决定时给出具体问题、影响与推荐方案。

如果必须中断，保存可恢复的本地工作并说明分支、提交、未完成改动、实际阻断、需要的信息和恢复后第一步。中断不等于目标完成；不得把未验证、跳过或受阻项目标为已通过。

现在核对最新本地状态，简短说明实际基线和第一项 M2 交付，然后开始实现。
```

**使用与判断边界**

这段提示词把产品能力作为持续目标，将里程碑作为内部检查点；它不能消除模型、会话、工具或运行环境的实际限制。需要恢复执行时，让 Codex 读取现有目标与状态文件，从记录的下一项继续，不再发送旧的 M0/M1 启动任务。

验收记录中的公司真实模型、工业接口、目标硬件容量以及跨浏览器集中采集仍未验证或实现，已在目标中明确后续任务与证据要求。示例创建测试按设计跳过，应按其实际用途判断；不能仅凭一项跳过就否定 M1，也不能凭通过数量推断整个产品已经可靠。
