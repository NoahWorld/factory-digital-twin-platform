# NewPower 数字孪生引擎：Codex 研发接手与持续开发指导

> 当前持续研发目标见 `NewPower_Codex_Product_Goal.md`；该文件已替代本文“首轮止于 M0 + M1”的终点。本文其余有效工程约束与历史基线继续保留。

版本：2026-09-12 · 本地研发与提交版。本文替代此前同名手册，是本次交给 Codex 的完整入口；无需同时提供之前的聊天记录。

仓库：[NoahWorld/factory-digital-twin-platform](https://github.com/NoahWorld/factory-digital-twin-platform)。开发分支：`newpower`，全部小写、没有空格。本文核对的远端提交：`bdf3905997f1a9301b97445a2917622b3430dc6b`。实际开工以用户电脑上的源码、分支和未提交工作为准，记录当时起点，不强行退回这个历史提交。

**已确认的工作方式**

- 这是公司已有的产品，公司同事主要在其他分支继续研发；`newpower` 已交给用户推进。用户以工作室身份开展本分支的产品研发。
- 在 `newpower` 持续实现功能、优化已有部分、运行和验证，按有意义的小任务创建本地 commit。
- **不 push 到任何远端，不创建远端 PR，不向其他分支交付。** 这条限制持续有效，除非用户以后明确改变；不要在每轮完成时反复询问是否推送。
- 一个用户、一个 Codex 主会话即可开始；需要时在同一设备增加只读复核任务，不要求第二个人或第二台电脑。
- 首次执行连续完成 **M0 接手与运行基线 → M1 通用数据绑定**。通过 M0 后直接进入 M1，不把整理文档当作本次产品开发的终点。

本文交付的是研发指令与验收依据。本文编写阶段进行了源码与文档核对，没有在用户电脑上安装、启动或验证产品；下文的验收均是接手 Codex 必须实际完成的工作。

**给用户：怎样把这份文件交给 Codex**

1. 下载并阅读本文，准备自己的本地项目目录。
2. 在 Codex 中打开该目录，选择 **Local／本地** 执行位置和你已经可用的 Astra。Local 会直接在当前项目目录工作，本方案不要求建立额外 worktree。[Codex 执行位置说明](https://learn.chatgpt.com/docs/environments/modes)
3. 把本文放到项目目录，或作为文件交给该会话。文件名保留 `NewPower_Codex_Development_Start_Guide.md` 即可。
4. 发送下方的“首次开工指令”。Codex 负责准备依赖、启动、实现、检查、修复与本地提交，你负责查看实际运行结果并提供业务反馈。

如果电脑上还没有仓库，在终端逐行执行以下命令，然后用 Codex 打开新目录：

```bash
mkdir -p ~/Projects
cd ~/Projects
git clone --branch newpower --single-branch https://github.com/NoahWorld/factory-digital-twin-platform.git factory-digital-twin-newpower
cd factory-digital-twin-newpower
git branch --show-current
git status --short --branch
```

分支检查应输出 `newpower`。目录已存在时不要重复克隆到同一路径，直接交给 Codex 检查。如果不想手动执行命令，也可以用 Codex 打开你选择的父目录，让它在一个不存在的新子目录中完成同样的克隆，再打开克隆后的项目目录继续。

如果已有自己的仓库目录，优先使用它，让 Codex 先检查本地修改与提交。新克隆不包含旧目录中未提交或未推送的成果。本文放入仓库后可以随首次文档整理纳入本地提交；已有同名文件则更新，不保留多份互相冲突的指令。

选择 Astra 后即可开工，无需先搭建 Agent 管理系统。CLI 用户可以从项目根目录执行：

```bash
codex -m gpt-6-astra
```

该模型标识来自当前官方文档；客户端中使用账户已经可用的 Astra 即可。[Codex 模型说明](https://learn.chatgpt.com/docs/models)

**首次开工指令：直接复制发送**

```text
请完整阅读 NewPower_Codex_Development_Start_Guide.md，并把它作为本次产品研发任务的说明。请直接实施，不要只给建议或路线图。

已确认：newpower 交给我推进，公司同事主要开发其他分支。我的目标是把它逐步完善为公司自用的完整数字孪生项目搭建引擎，替代并在实际项目中超越 EasyV 与 EasyTwin。

请按文件执行 M0，并在启动与必要基线验证通过后直接进入 M1，完成通用数据绑定的可运行结果、相关检查和本地提交。本轮以 M0 + M1 的完成标准为范围；不要停在环境准备或文档产出，也不要提前展开全部后续里程碑。

允许完成本轮需要的本地代码和文档编辑、依赖准备、本地迁移、服务启动、相关测试、浏览器检查、修复和本地 commit。研发所需的公开网页及云浏览器访问已经允许，不要就同类访问反复申请许可；具体工具若受到系统限制，准确报告并继续可行工作。

只在 newpower 开发和提交，不 push，不创建远端 PR，不部署外部生产环境，不修改远端数据库，不修改或合并其他分支。main 如有需要仅只读参考。不要把推送、合同安排或再次确认分支归属作为本地研发的前提。

先检查真实工作区并保护已有成果。旧 AGENTS 或产品方案若要求默认 main、禁止本轮已授权的通用引擎能力，按本次明确范围执行，并在 newpower 作简明适用说明；保留有效工程约束。

过程中的普通技术选择自行判断，发现可修复问题就修复并复验。只有缺少会实质改变实现的业务信息、必要凭据或存在不可逆操作时，才提出具体问题，同时继续不依赖答案的工作。

最后给我：实际运行地址、可以亲自操作的验收步骤、功能变化、实际检查结果、本地提交、尚未完成项和下一步。不能把未执行的检查标记通过。
```

**产品目标：做成可以反复搭建项目的引擎**

最终使用方式是：团队在平台中创建项目、导入模型和资源、搭建二维页面与三维场景、连接数据、配置交互、预览调试、生成可交付版本，再持续修改和升级。优先用工厂设备场景打通全流程，将可复用机制沉淀到引擎中，业务案例通过项目配置、组件、模板或适配器实现。

“超过当前 main”可以作为参考比较，“替代并超越 EasyV 与 EasyTwin”是长期产品目标。应通过同一类真实项目的搭建和运行验证这些目标；一个漂亮大屏或一个固定场景只能证明对应案例可用。

| 产品能力面 | 引擎最终需要支持的行为 |
|---|---|
| 项目与二维编辑 | 项目、页面、图层、组件、布局、属性、模板；选择、组合、复制、撤销、保存与恢复 |
| 三维编辑与资产 | 多模型、多实例、场景树、变换、材质、光照、镜头、动画、资源复用和版本更新 |
| 数据与语义 | 设备与资产身份、数据源、字段与类型、转换、绑定、共享采集、实时与历史数据、运行诊断 |
| 交互与业务 | 选择、事件、条件、动作、状态、镜头与模型联动；交互能配置、调试、保存与复用 |
| 编辑与运行 | 编辑器预览和交付运行器执行一致的项目定义；运行数据与项目配置分别管理 |
| 交付与维护 | 导入导出、依赖校验、版本迁移、资源打包、发布版本、回滚、目标内网环境运行 |
| 扩展与 AI | 可注册组件、数据适配器和动作；Astra 通过可验证、可撤销的操作接口辅助搭建 |
| 产品质量 | 清晰操作流程、旧项目兼容、权限与凭据管理、失败提示、性能量测、稳定运行和故障定位 |

这是内部自用产品，优先满足团队真实交付流程。当前研发无需以建设商业 SaaS 的计费、套餐、公开市场为前提；既有账户、项目权限与数据隔离能力仍须保留和完善。

**给接手 Codex：执行边界与默认判断**

1. 唯一写入分支为本地 `newpower`。读取远端信息、克隆或获取 `newpower` 更新可以进行；获取更新不等于自动整合，已有本地提交时先比较。不要因为远端出现变化就覆盖、重置或自动变基本地成果。其他分支不修改、不自动合并，也不创建额外研发分支。
2. 允许为产品需要进行有依据的结构调整。以支持本轮可验收能力为范围，保留兼容路径；根据代码证据决定复用、抽取或替换，不机械保留缺陷，也不先推倒全部系统。
3. 对 `AGENTS.md` 作简短、明确适用于 `newpower` 的补充：本地开发、本地提交、不推送、引擎目标、常用检查入口。保留密钥保护、服务端校验、稳定身份、资源与配置分离等规则。公司的既有方案保留其背景，NewPower 的目标和状态独立标明；启动命令等错误事实可直接修正。[AGENTS.md 工作方式](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
4. 不创建远端 PR、发布 Release 或进行外部正式部署。产品自身的“发布、打包、部署适配”能力可以开发，并在本地或已授权的隔离环境验证；实现发布功能不等于获准发布现有系统。
5. 复用本地模拟设备和测试模型，明确标注模拟数据。缺少真实设备或公司模型时，继续验证可通用的链路；需要真实环境才能验证的部分单独记录，不捏造验证结果。
6. 浏览器优先使用能访问开发服务的本地 Playwright 或等效工具；云浏览器只能访问其实际可达的地址。不要把云浏览器的 localhost 当成用户电脑，也不要为访问开发页面而默认公开公司数据。首次需要补装本地浏览器测试依赖可按本轮授权执行。
7. 一个主会话统一写入。可并行委派源码调查和只读审查，但不要让多个独立会话同时修改同一工作目录。主会话负责整合和最后复验。
8. 同一任务的安装、启动、实现、检查和修复持续完成，不在每一步索要“是否继续”。碰到明确权限拒绝或真正缺失的信息时，说明具体受阻操作、影响及可继续部分；不绕过工具限制。

**已经核对的源码基线：开工时复核，不从头重做调研**

以下结论来自本文所列提交的源码阅读，不能替代本地运行验收。

| 范围 | 当前可复用的基础／待处理问题 | 首轮影响 |
|---|---|---|
| 平台骨架 | 已有账户、项目、画布节点、模板、模型资源、设备资产和数据采集相关实现 | 以现有工程继续产品化，先验证真实可用程度 |
| 普通二维组件 | 主要消费 `node.props` 的静态数值 | M1 打通绑定与运行消费者，保留静态演示模式 |
| 绑定字段 | 存在 `dataBindingRefs`，但不足以代表通用绑定已经实现 | 需要可保存、可校验、能被组件执行的完整契约 |
| 前端采集 | `CanvasPage` 从模型节点获取资产；现有路径限制恰好一个 3D 节点、最多 50 个模型资产 | 先解除二维运行对模型的依赖；限制属于这条实现路径，不是数据库或整个引擎的硬上限 |
| 后端采集 | 已有资产指标映射和 REST 采集逻辑 | M1 优先复用；不因解耦前端而重建全部采集服务 |
| 工程规则 | 历史文档含默认 main 与阶段性限制通用编辑器的描述 | 补充本轮 newpower 适用规则，避免阻断已授权工作 |

源码入口：[CanvasPage.tsx](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/apps/web/src/pages/CanvasPage.tsx)、[runtime-state.ts](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/apps/web/src/runtime-state.ts)、[AGENTS.md](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/AGENTS.md)。目录与类型发生变化时，以开工源码为准，修订记录后继续。

**M0：接手、启动、确认真实基线**

按依赖顺序完成以下工作。M0 的文档保持精简，实施与验证是主体。

1. 检查仓库地址、当前分支、HEAD、未提交内容和本地未推送提交。保护已有成果；可以安全进入时使用 `newpower`。记录实际起始 SHA，不把他人的已有改动纳入自己的成果说明。工作区不适合直接切换时，保留原目录并使用独立克隆，不丢弃修改。
2. 读取适用规则、包管理配置、锁文件、启动文档和关键链路。建立 `docs/newpower/development-status.md`；已有等效文档则复用。在根规则中留下简短入口，不把这整份手册复制进 AGENTS。
3. 准备兼容的 Node 与 pnpm，按锁文件安装。先构建 web，再启动依赖 `web/dist` 的 API；完成全部本地迁移，保护已有配置和数据库。准确命令见文末参考。
4. 启动前端、API 与 mock。用隔离测试环境检查初始化或登录、创建项目、应用模板、编辑保存、刷新重开、测试模型加载及既有设备联动。
5. 运行已有类型检查、构建与适用的 API smoke。在浏览器验证模型实际渲染、交互和数据状态变化；补上原 smoke 未充分覆盖的断连恢复检查。记录真实结果与必要截图或日志。
6. 修复阻断本轮工作的问题并复验。接手时存在且与本轮无关的问题登记到待办；与本轮功能直接相关的兼容性、资源释放、权限和错误展示问题一并处理。
7. 创建本轮相关的本地提交，写明接手基线、环境、已验证能力和遗留问题。基础链路和 M1 必要前提通过后，直接开始 M1。

M0 达标意味着：开发环境能复现，已有关键链路的状态有实际证据，本地提交与适用规则明确。确实阻断启动时如实标为未完成，继续排除可处理的问题；不能因为报告写完就宣告达标。

**M1：项目级运行数据与通用组件绑定**

目标：同一份标准化设备数据驱动指标卡、柱状图、业务表格和现有三维状态；只有二维组件的页面也能独立工作。用户能通过属性面板完成绑定，保存并重新打开后仍有效。

使用两台明确标为模拟的设备验证，保留 `DEVICE-001` 的旧测试兼容。柱状图先比较两台设备的同一指标，例如温度；本轮无需为了画图额外建设历史数据库。

| 工作项 | 必须完成的行为 |
|---|---|
| 绑定契约与保存 | 区分静态演示与真实绑定；定义资产选择、指标、类型、目标属性，支持固定资产与当前选中资产；绑定能保存、加载、校验和删除 |
| 运行数据层 | 按项目和实际组件需求管理采集、缓存及选择状态；同一运行页面内相同需求共享请求，卸载时释放；三维映射只负责模型关联，不再决定全部数据订阅 |
| 编辑体验 | 属性面板可以选择资产与指标，预览结果；显示绑定错误位置和原因；用户不需要编辑源码才能配置 |
| 二维消费 | 指标卡显示当前设备指标，柱状图比较两台设备，表格展示设备与指标；支持没有 3D 节点的页面 |
| 二维三维联动 | 点击三维设备更新二维详情；点击表格设备可高亮或定位已有映射对象；无映射设备仍能在二维使用 |
| 异常与生命周期 | loading、empty、null、类型不符、陈旧、断连和恢复有确定行为；绑定失败不静默替换成演示数字；运行变化不使编辑配置变脏 |
| 权限与兼容 | 服务端校验项目归属、绑定引用和读写权限；旧静态模板、旧设备映射、旧项目仍能打开、修改和保存 |

实现前简述数据流、持久化方式和旧配置兼容策略，随后直接实施。绑定定义可沿用或补齐现有存储结构，画布引用必须能解析并通过校验；不要仅增加一个类型或字段就认定功能完成。模型绑定使用稳定资产与对象身份，展示名不能承担唯一身份。

“当前选中资产”必须有真实用户入口，例如表格点击或设备选择控件，在纯二维页面也能改变选择。共享订阅先覆盖同一项目、同一运行页面；跨浏览器统一采集、长连接与生产调度属于后续能力，不能由前端去重成功推断已经具备。

M1 必须实际验证以下场景，并在失败后修复复验：

1. 纯二维项目配置两个设备，指标卡、柱状图和表格均随模拟数据变化；删除 3D 组件后，所需的二维数据订阅仍有效。
2. 保存、关闭、重开，绑定仍在，类型与设备选择正确。
3. 带三维映射的项目中，二维与三维共享数据和选择状态；快速切换设备不串线。
4. 两个组件订阅同一资产时不会造成无意义的重复采集；切换项目与卸载页面释放旧订阅。
5. 让接口故障、数据停止更新，再恢复；界面正确展示失败或陈旧，并自动恢复；空值与类型错误不统一变成 0。
6. 无权限或跨项目绑定被服务端拒绝；旧模板和已保存项目通过相关回归。

本轮最小展示成果是一套可操作的双设备示例，附纯二维验证步骤和二维三维联动步骤。示例通过通用绑定实现，不能把设备 ID 和业务数值硬编码进公共组件。技术验证完成后留下本地提交、运行地址、操作说明、验证证据与后续缺口。

**后续路线：每轮交付一个完整能力，再继续扩展**

首轮结束后，按依赖与用户反馈选择下一任务。编号表示研发方向，不是固定工期；必要依赖可提前完成。将大阶段拆成几次可验证的工作，不把全部路线一次性展开。

| 阶段 | 研发重点 | 可验收的产品结果 |
|---|---|---|
| M0 | 接手与运行基线 | 本地能运行，旧链路状态清楚，接手基准可追溯 |
| M1 | 通用运行数据与绑定 | 双设备驱动二维组件与三维，纯二维也可运行 |
| M2 | 编辑器与项目文档 | 稳定实体 ID、schema 版本与迁移、撤销重做、多选分组复制、多页面、脏状态与保存恢复；旧项目迁移可编辑 |
| M3 | 三维场景与资源 | 多模型与实例、场景树、变换工具、材质灯光、镜头、资源版本和复用；替换模型后可诊断并修复绑定 |
| M4 | 动画与交互编排 | 事件、条件、动作、状态、模型动画、镜头或路径；用户可配置并调试二维三维联动 |
| M5 | 运行器与交付 | 编辑配置生成可运行项目；资源依赖检查、导入导出、版本回滚、本地独立运行包与内网部署方案验证 |
| M6 | 数据与运行完善 | 真实接口鉴权、共享连接、持续采集、按项目需要增加 WebSocket/MQTT/数据库适配、历史和告警对接、诊断与权限完善 |
| M7 | 规模与专业场景 | 在目标硬件量测大模型和多设备负载；按需求补压缩、LOD、实例化、GIS、倾斜摄影或高斯资源支持 |
| M8 | AI 辅助搭建 | Astra 通过受校验的编辑操作创建和修改项目；给出差异、验证和修复，用户可撤销；用固定任务评价成功率 |

每阶段同时优化被触及的既有实现：清理重复状态、补错误路径、处理资源释放、改善操作反馈，并验证旧项目。单独的大规模重构需有明确收益、迁移步骤与回归证据，不以增加抽象层数量作为完成标准。

保持几个清楚的结构边界：可序列化的项目定义；编辑操作与撤销；运行数据与交互执行；渲染组件与资源；后端权限与存储。先通过 M1 等真实需求建立必要边界，逐步抽取稳定接口，不预先生成无人使用的框架。

“发布能力”开发可以先在本地完成，即使当前仍然不 push。项目在真实内网机器上的兼容性，需要对应环境或等效验证证据；尚无环境时列为待验证项。

如业务需要工程仿真，应另外明确模型、输入输出、求解器和验证数据。状态变色、动画和漫游属于可视化行为，不能据此声称物理或离散事件仿真已实现；可以通过接口连接专门求解器。

Astra 在研发阶段从第一轮就用于分析、实现、检查和修复。把 AI 接入引擎产品是另一个能力阶段：优先让人工与 AI 共用受校验、可撤销的操作接口，再增加自然语言入口；不把添加聊天框当成引擎能力完善。

**怎样验证确实更好**

将竞品对标落实到固定场景：创建项目、导入资源、接入两个设备、配置联动、修改模型或数据字段、交付、重新打开和恢复故障。记录完成步骤、人工改源码的次数、搭建用时、错误提示、恢复行为及目标硬件的帧率、内存、加载时间。

M0 先保留本项目起始版本的可复现记录，后续每轮在相同条件下比较。性能指标须写明硬件、浏览器、模型规模、设备数量和采样方法；本手册不虚构通用的 FPS 或容量保证。

对 EasyV 与 EasyTwin 的每项能力分别标记：公开资料确认、实际操作确认、尚未验证。对 NewPower 则标记：已实现且验证、已实现待验证、部分实现、未实现。未实测的竞品行为不写成确定缺陷，规划不记成现有能力，不用随意估计的百分比宣布超越。

无需每轮重新研究整个竞品。遇到明确能力缺口时再做有针对性的文档或浏览器调查；把发现变成当前任务的具体操作与验收条件。

**本地提交与成果记录：本阶段始终不 push**

- 每完成一个有解释价值的小任务，检查差异、暂存相关文件并 commit。使用真实作者身份；缺少身份时只补当前仓库所需配置，不冒用公司同事，不随意修改全局配置。
- 不等待整个引擎完成才提交；未完成的检查点如需保存，明确标注状态，不能作为已验收功能交付。
- 每次提交前确认当前分支为 `newpower`，提交内容不含密钥、运行数据库、无关生成物或他人未归属改动。无需为了小文案修改反复执行全套测试，功能交付运行必要的相关检查。
- 不执行任何 push，也不改写已经共享的提交历史。`fetch`、源码阅读和本地 commit 都不构成远端交付；本地累计领先远端是本阶段的正常状态。
- 需要备份时使用用户选定的位置。本地 commit 不是异地备份，不自动上传其他私人仓库或外部存储。运行数据库、未跟踪资源和模型不一定包含在 Git 提交中，按实际来源记录恢复方法。
- 每批成果记录任务、起始 SHA、目标 SHA、验证和遗留问题。目标提交生成后在交付说明中记录，不要求把提交自己的哈希写入该提交。

长期记录保持少而准确，建议沿用或建立以下入口：

| 文件 | 内容 |
|---|---|
| `AGENTS.md` | 简明工程约束、newpower 适用范围、禁止推送、常用命令与文档入口 |
| `docs/newpower/development-status.md` | 当前任务、开工基线、完成证据、未完成项、下一步；新会话优先读取 |
| `docs/newpower/roadmap.md` | 完整产品方向、已完成与后续能力，按实际进展更新 |
| `docs/newpower/acceptance.md` | 实际执行的验收步骤、结果与证据位置；区分技术验证与用户体验反馈 |

如果已有等效文件，复用并记录真实路径，不为满足文件名机械复制四份。大型截图、日志与样例资源按仓库惯例管理；文档不能成为每轮实施前的冗长审批过程。

**后续如何继续：复制一段即可**

同一任务尚未完成时：

```text
继续当前 NewPower 任务。沿用既定授权，只在 newpower 本地开发、验证和 commit，不 push。
读取当前任务记录和实际工作区，接着处理未完成项，修复并复验直到达到本轮完成标准。
不要从头重做调研，不要因为完成文档或第一次构建通过就停下。
```

M1 完成后进入下一阶段时：

```text
继续 NewPower 产品研发。先结合已完成能力、当前问题和路线图，选择下一项依赖已具备、能形成完整用户操作的任务。
用一小段说明本轮目标、范围和验收步骤，然后直接实施、运行、检查、修复并本地提交。
同时处理这条链路触及的旧问题，验证旧项目兼容。普通实现选择自行决定，只有会改变产品方向的关键选择才提出具体问题。
只在 newpower 工作，不 push，不修改其他分支，不部署外部生产环境。
```

会话中断或上下文过长时，将进度、未完成改动、运行中的服务和下一步写入状态记录。新会话读取这些记录并核对实际代码后接着做。文件让后续会话可恢复，不保证模型在会话结束或电脑关闭后自动继续运行。

需要复核时，在同一设备开一个只读任务：

```text
请只读审查本轮 NewPower 改动，以主研发提供的明确基准提交和目标提交为范围。
核对任务完成标准，查找真实功能遗漏、旧项目回归、权限问题、错误路径、资源或订阅泄漏，以及测试未覆盖的行为。
不要编辑文件、提交、切换分支或使用共享数据库写入测试数据。
按严重程度列出可复现问题、代码位置、影响和修复建议；没有发现问题时如实说明，并写清审查范围。
```

**每轮交付给用户的内容**

```text
本轮目标与完成状态：
实际运行地址与启动方法：
用户可以亲自执行的操作步骤：
新增或改善的产品行为：
已运行的检查、结果与证据：
旧项目兼容情况：
起始提交与本轮本地提交：
未完成内容与原因：
下一项具体任务：
```

用户重点检查：功能能否在页面上操作；保存重开是否保持正确；异常是否能识别和恢复；结果是否有验证依据；本地提交是否清楚。代码行数、提交数量和文档篇数不代替产品完成度。

**本仓库准确的本地启动参考**


下面这些命令用于理解和排查，也可以直接交给 Codex 执行。它们来自本次核对的源码；未来依赖更新后，以当前锁文件为准。

| 项目 | 已核对的事实 |
|---|---|
| 工作空间 | pnpm workspace，`apps/web` 与 `apps/api` |
| 包管理器 | 根 `package.json` 固定 `pnpm@10.0.0` |
| Node 要求 | 锁定的 Vite 7.3.6 要求 `^20.19.0 \|\| >=22.12.0`；Wrangler 4.111.0 要求 `>=22.0.0`；选择两者同时满足的版本，例如 22.12+ |
| 前端 | React、TypeScript、Vite、Three.js |
| API | TypeScript Worker、本地 D1/R2 绑定 |
| 首次构建顺序 | 先 web；Worker 静态目录是 `../web/dist` |
| 数据库迁移 | 当前目录包含 0001–0011；执行全部本地迁移 |
| 默认端口 | Vite 5173；Worker 8787；mock 8790；实际地址以进程输出为准 |
| API 代理 | Vite 将 `/api` 代理到 `http://127.0.0.1:8787`，可通过 `VITE_DEV_API_TARGET` 调整 |
| 本地环境文件 | `apps/api/.dev.vars.example`；当前没有 `apps/web/.env.example` |

核对来源：[根 package.json](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/package.json)、[锁文件](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/pnpm-lock.yaml)、[Worker 配置](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/apps/api/wrangler.jsonc)、[Vite 配置](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/apps/web/vite.config.ts)。

首先检查 Node 和 pnpm。若工具缺失或版本不满足要求，让 Codex 根据你的现有安装方式补齐，并记录实际版本；不需要你自行研究多个版本管理器。

```bash
node --version
pnpm --version
pnpm install --frozen-lockfile
```

依赖安装失败时，应诊断 Node、pnpm、网络或锁文件问题；不能直接删除锁文件、升级所有依赖后把结果当成同一基线。

创建本地配置时，只有目标文件不存在才复制模板：

```bash
if [ ! -f apps/api/.dev.vars ]; then
  cp apps/api/.dev.vars.example apps/api/.dev.vars
fi
```

让 Codex 在本机将模板中的 `BOOTSTRAP_TOKEN` 占位值替换为足够长的随机值，保护已有配置，不把值回显到报告或提交 Git。当前本地示例的其他设置为：

```dotenv
SESSION_TTL_HOURS=24
RUNTIME_POLLING_ENABLED=true
RUNTIME_ALLOWED_HOSTS=127.0.0.1:8790
```

标准本地前端通过同源 `/api` 代理访问服务，一般无需额外设置 API 基址。需要改变端口或地址时，同步确认 Vite 代理和实际 Worker 地址，不在组件里硬编码。

执行前端构建与本地数据库迁移：

```bash
pnpm --filter @factory-twin/web build
pnpm --filter @factory-twin/api db:migrate:local
```

该迁移命令应用整个迁移目录。不要照旧 README 的枚举只执行到 0009；当前迁移已到 0011。`db:migrate:remote` 是远端操作，不属于这里的本地启动步骤。

然后在三个终端分别启动，不能把三个长期运行进程写成依次等待退出的一个启动链：

```bash
# 终端 A：API
pnpm dev:api
```

```bash
# 终端 B：前端
pnpm dev:web
```

```bash
# 终端 C：模拟设备
pnpm dev:mock
```

默认前端地址为 `http://localhost:5173`，mock 设备地址为 `http://127.0.0.1:8790/device/DEVICE-001`。服务启动后，以控制台给出的实际地址为准。端口被占用时先识别已有服务，不随意结束其他项目的进程。

首次打开前端并进入平台，空用户库会显示初始化管理员界面。你可以在本机输入初始化令牌，自设邮箱、显示名和密码；已经初始化过则使用正常登录，不存在通用默认账户。自动验收应使用隔离测试库及临时账户，不需要获取你的生产密码。

现有检查命令为：

```bash
pnpm check
pnpm build
```

其中 `pnpm check` 是 TypeScript 检查，`pnpm build` 中 API 部分执行 `wrangler deploy --dry-run`，不会因此完成正式发布。当前没有通用 `pnpm test` 或现成 Playwright E2E 脚本；新增关键链路时，可以由 Codex 建立所需自动化检查，但必须实际运行后再报告结果。[API 脚本](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/apps/api/package.json)、[前端脚本](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/apps/web/package.json)

**现有 smoke 测试有一个必须理解的条件**

`pnpm smoke:runtime` 会根据临时初始化令牌派生测试账户，创建项目、生成测试 GLTF、上传模型、配置资产及数据源，检查数据采集、陈旧、故障和恢复，然后默认删除测试项目。它不会自动替你启动所有服务。

测试覆盖也有边界：现有脚本会断言数据由陈旧恢复为新鲜，但在 503 场景之后主要通过清理逻辑恢复 mock，没有再次采集来断言故障后的完整恢复。本轮应补上对应验证，或在浏览器中明确检查并记录。`GET /health` 主要检查绑定配置，也不能单独证明数据库迁移成功。

如果直接在你已经手动初始化管理员的常用库上运行，测试账户可能不存在，导致登录失败。因此，应让 Codex 建立独立临时数据目录，在该目录应用全部迁移并启动独立 Worker，例如使用 8791 端口；迁移和 Worker 必须使用同一个绝对 `--persist-to` 路径。临时测试令牌由本机自动生成，测试 Worker 实际读取的 `BOOTSTRAP_TOKEN` 必须与 smoke 脚本使用的值一致；使用测试专用配置或启动方式传入，不覆盖日常 `.dev.vars`，不在日志或报告中回显令牌。

临时 Worker 如果使用 8791，默认 5173 前端仍然代理日常 API 的 8787。浏览器验收必须指向同一个临时环境：为测试前端将 `VITE_DEV_API_TARGET` 配为临时 Worker 地址并使用空闲前端端口，或者直接访问临时 Worker 承载的已构建前端。记录实际访问 URL，检查请求确实落到测试 Worker；不要把日常开发库的页面当作 smoke 测试结果。

测试已有 `--keep-project` 选项，可以为浏览器检查保留测试项目。浏览器还需要验证模型是否实际渲染、节点是否可选、界面状态是否正确；不能由 API smoke 成功直接推断这些行为。[现有 smoke 脚本](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/scripts/runtime-api-smoke-test.mjs)、[现有本地联调文档](https://github.com/NoahWorld/factory-digital-twin-platform/blob/bdf3905997f1a9301b97445a2917622b3430dc6b/docs/local-rest-device-runtime-test.md)


**常见阻断的处理参考**

| 现象 | Codex 应执行的检查 |
|---|---|
| 仍要求在 main 开发或拒绝本轮通用引擎工作 | 核对实际生效规则，补上明确适用于 newpower 的本任务说明，保护其他有效约束 |
| 依赖安装失败 | 核对 Node、pnpm、网络、锁文件；不先删除锁文件或批量升级 |
| API 启动找不到静态目录 | 先构建 web，核对 Worker 的静态资源目录 |
| 数据库缺表或登录失败 | 核对完整迁移、Worker 实际数据目录、开发库与 smoke 临时库是否混用 |
| 图表有数字但不随设备变化 | 区分演示 props 与绑定模式，检查消费者、订阅、字段映射和异常状态 |
| API smoke 通过但模型黑屏 | 检查 WebGL、资源请求、场景与控制台，在浏览器实际验证 |
| 云浏览器无法访问 localhost | 使用能访问该开发机器的浏览器；准确报告可达性，不伪造验收结果 |
| Codex 只写文档、不推进功能 | 继续执行当前阶段的实际操作与验收条件，直到可运行结果完成或有明确阻断证据 |

本文的开发与提交授权可以持续沿用。用户之后调整目标或优先级时，更新当前任务记录并继续推进；不需要重新讨论已确认的分支归属或反复申请推送。
