# Use the locally verified JAR, avoiding Maven downloads inside Docker.
services:
  api:
    build: {context: ../../apps/backend, dockerfile: Dockerfile.runtime}
  collector:
    build: {context: ../../apps/backend, dockerfile: Dockerfile.runtime}
  worker:
    build: {context: ../../apps/backend, dockerfile: Dockerfile.runtime}
