# NewPower 实际验收

## M0（2026-09-12）

起点：`bdf3905997f1a9301b97445a2917622b3430dc6b`。本机 Node 24.18.0、pnpm 10.0.0、Chrome，由 Playwright 1.62.0 操作；视口 1440 × 1000。

| 检查 | 实际结果 |
| --- | --- |
| 工作区导入与分支 | 当前目录空目录克隆 `newpower`，导入检查 0 问题 |
| 锁文件安装 | 通过；增加 Playwright 用于本轮浏览器回归，原应用依赖版本保持 |
| `pnpm build` | 修复首次并行顺序后通过；API 为 dry-run，未部署 |
| `pnpm check` | web/API TypeScript 均通过 |
| 本地迁移 | 独立测试库应用 0001–0011 全部通过 |
| API smoke | 12 项通过，涵盖认证、GLTF 上传、字段发现、标准化数据、陈旧与故障恢复 |
| 浏览器基线 | 2 项通过：登录→创建→模板→保存重开；模型渲染/点击→指标→陈旧→失联→自动恢复 |

证据目录为 `/Users/inosaki/Documents/Codex/local/runtime/newpower-01a095a6/`：`m0-build.log`、`m0-migrations.log`、`m0-smoke.log`、`m0-browser.log` 和 `m0-browser/` 截图。模型为 smoke 生成的自包含模拟立方体，不代表公司模型或性能量测。

实际服务：前端 `http://127.0.0.1:5173` 代理至隔离 Worker `http://127.0.0.1:8791`；mock 为 `http://127.0.0.1:8790`。测试 Worker 配置与状态分别位于该证据目录的 `test/wrangler.jsonc`、`test/state/`；测试会话与临时凭据仅在权限 0600 的 `session.json`，不提交。

浏览器复验：设置 `NEWPOWER_SESSION_FILE` 指向该私有会话文件，运行 `pnpm exec playwright test tests/baseline.spec.ts`。服务须已启动，默认连接本机 5173；可用 `NEWPOWER_BASE_URL` 和 `NEWPOWER_MOCK_URL` 指向同一隔离环境。

M0 只证明旧链路；新增能力见下方 M1 实际验收。

## M1（2026-09-12）

功能提交：`463c1ab`，基线提交：`ccf3e1a`。最终原生浏览器为 Chrome 152.0.7977.83，两套同源构建示例已实际登录、选择设备并读回实时值。

新增本地迁移 `0012_component_data_bindings.sql` 已成功应用。绑定定义独立存储，引用、组件目标、指标类型、归属与整个画布版本共同校验；失败保存不改变现有版本。标准化指标保留 `null`，运行快照和设备选择不修改静态 props。接口与保存的可执行样例见 `tests/demo.ts`，共享契约和具体校验信息见 `shared/component-bindings.ts`。

| 验收场景 | 实际结果 |
| --- | --- |
| 纯二维界面配置 | 在属性面板为固定指标卡、当前设备指标卡、柱状图和表格配置绑定；保存、刷新、预览、移除绑定及恢复静态均通过 |
| 无模型设备 | 通过“资产与指标”新增设备、选择数据源、配置指标并绑定二维组件；实际采集通过 |
| 双向联动 | 表格选择高亮对应模型；三维点击更新指标卡、表格和设备选择器；模型迟到加载会补齐高亮 |
| 移除三维 | 从编辑器删除 3D 组件后保存，二维组件仍正常采集双设备数据 |
| 需求和生命周期 | 同资产多组件共享请求；已有缓存不因重复需求重置；并发有界、迟到结果被忽略；切换项目和离开页面释放原请求与定时器 |
| 异常和恢复 | 真实 mock 返回 null、错误类型、缺失字段、旧时间戳和 HTTP 503；界面不回退示例或变为 0，恢复后自动显示实时数据 |
| 单位 | 表格按每台设备显示各自单位；柱状图对不一致单位明确报错 |
| 服务端边界 | 外项目引用、缺失引用、错误目标/类型、未知字段被拒绝；旧版本保存返回 409；只读成员可读不可写，无项目权限及匿名读取被拒绝 |
| 旧功能回归 | 原静态模板、旧单设备三维、保存重开、旧版 API smoke 均通过 |

综合运行 `pnpm test:newpower`：**13 项通过，1 项示例创建入口按设计跳过**（`acceptance.log`）。随后完成表格逐设备单位显示的 1 项专项复验（`units-recheck.log`）；功能检查合计 14 项。单位专项还校验表格外框遵守画布配置高度，避免表格与外框同名样式相互覆盖；对应最新复验见 `final-layout.log`。`pnpm check` 和最终 `pnpm build` 通过，API 构建仍为 dry-run。旧版 smoke 的 12 项检查复验通过（`m1-legacy-smoke.log`）。

完整回归在构建后的 `http://127.0.0.1:8791` 进行；专项也验证了 5173 开发页面。`acceptance/` 保存真实 UI 截图和相关失败时的 trace；`m1-model/` 保存选择框、独立设备告警颜色及删除三维后的截图；`m1-final-recheck/` 保存独立资产配置操作。失败的旧检查保留原始日志，已通过上述修复复验，不记为未解决缺陷。

## 亲自操作

1. 打开 [纯二维示例](http://127.0.0.1:8791/#/projects/ead94aa6-63de-42b2-8930-0df588b88147/preview)，点击表格的“模拟设备 001/002”，观察当前设备指标卡切换；温度会随 mock 采集变化。
2. 打开 [二维三维示例](http://127.0.0.1:8791/#/projects/83549ca8-cdab-4e91-b778-1bd9bb9880b2/preview)，点击表格或三维设备，查看模型高亮、指标和表格选择同步。示例立方体是测试模型。
3. 点击“返回编辑”，选择指标卡、柱状图或表格，在右侧“数据绑定”选择设备和指标，点击“应用到组件”，再“保存画布”。刷新重开后绑定保留。
4. 工具栏“资产与指标”用于添加无模型设备及配置指标；“数据源”沿用原 REST 连接配置。模拟源只用于本机联调，不表示真实设备已接入。

如需重新登录，本机隔离演示凭据只保存在证据目录的 `演示登录信息.md`（0600）；不得提交。整个运行目录权限为 0700。该账户不是公司生产账户。

## 继续运行和复验

所有命令在仓库根目录运行，通过 `/Users/inosaki/Documents/Codex/local/tools/dev-env` 使用工作区工具链。当前本机的运行目录记作 `NEWPOWER_RUNTIME`，值为 `/Users/inosaki/Documents/Codex/local/runtime/newpower-01a095a6`。以下独立终端命令重用既有配置与数据，不覆盖令牌：

```sh
# 先检查端口和当前项目的进程；已有正确服务时复用，不重复启动。
pnpm build
NEWPOWER_TEST_STATE_DIR="$NEWPOWER_RUNTIME/test/state" NEWPOWER_TEST_CONFIG="$NEWPOWER_RUNTIME/test/wrangler.jsonc" pnpm migrate:newpower
# API 与已构建前端
pnpm --filter @factory-twin/api exec wrangler dev --local --ip 127.0.0.1 --port 8791 --inspector-port 9231 --persist-to "$NEWPOWER_RUNTIME/test/state" --config "$NEWPOWER_RUNTIME/test/wrangler.jsonc"
# 另一终端：模拟设备
pnpm dev:mock
# 如需源码热更新，再开一个终端
NEWPOWER_POLL_WATCH=true VITE_DEV_API_TARGET=http://127.0.0.1:8791 pnpm --filter @factory-twin/web exec vite --host 127.0.0.1 --port 5173 --strictPort
```

测试前将下面的变量传给 `pnpm test:newpower`，不要打印会话或令牌内容：

```sh
NEWPOWER_BASE_URL=http://127.0.0.1:8791
NEWPOWER_SESSION_FILE="$NEWPOWER_RUNTIME/session.json"
NEWPOWER_TEST_STATE_DIR="$NEWPOWER_RUNTIME/test/state"
NEWPOWER_TEST_CONFIG="$NEWPOWER_RUNTIME/test/wrangler.jsonc"
NEWPOWER_ARTIFACTS_DIR="$NEWPOWER_RUNTIME/recheck"
```

这些是测试进程的环境变量，需要通过 `export` 或同一次命令传入。`tests/demo-setup.spec.ts` 仅在 `NEWPOWER_SEED_DEMOS=true` 时创建新的示例；已有示例可直接继续使用。修改前先核对服务与数据目录归属，不在用户真实数据库上跑权限夹具。

本轮没有实测公司真实模型、真实工业接口或目标硬件容量；页面内共享请求不等于跨浏览器或生产集中采集已实现。

## M2 编辑操作检查点（进行中的 M2）

起点 `8c9325f`。已将节点/主题类型、尺寸约束及纯校验移至共享模块，服务端读取/保存、编辑器加载和命令实际调用；M1 绑定的局部引用校验也已统一。编辑操作支持复制、删除、绑定修改、模板与主题更换、撤销重做；保存后的撤销保留最新服务端 revision。该检查点不是 M2 或整个目标的终点。

操作链“复制带绑定组件→删除→撤销/重做→保存重开→实时预览”通过。复制保留资源 ID、建立独立绑定 ID，删除和撤销对应恢复同一实体。只读复核发现的畸形命令、重复 ID、缺失/多重引用问题已补校验并复验。19 项综合回归通过（`m2-regression.log`）；新增边界的功能检查通过，性能及 API 解析的针对复验分别见 `m2-perf-recheck.log`、`m2-api-parser.log`。构建和类型检查通过。

性能环境为 Mac17,4、Apple M5、16 GiB 内存。25/100/500 组件的解析+复制操作各测 15 次；独立复验 p95 分别约 28.6/7.0/41.0 ms，100 ms 预算保持不变。这是文档操作 CPU 路径，不是渲染容量证明。并行构建期间曾出现 500 组件 146.9 ms 的失败样本（`m2-boundary.log`），该记录保留；计时已将测试断言开销分开，后续 M7 继续检查负载下尾延迟和长时表现。

下一项继续多页 schema 迁移、多选分组、跨页引用与草稿恢复，要求项目级 CAS、3D 返回所属页、默认封面来自入口页，以及旧 ID/绑定保持。


## M2 多页面与恢复验证（2026-09-13，提交前复核）

起点 `7e3edcb`。项目 schema v2 包含页面、稳定入口页与项目级绑定；共享解析用于 API、编辑器和预览。0013 将旧画布迁入 `main`，三个既有项目的节点、绑定和 revision 与迁移前 API 快照完全一致。`/canvas` 保留入口页/指定页兼容投影，完整定义使用 `/definition`。项目级 CAS、全项目引用验证与资源检查在一次事务中完成。

浏览器已验证页面新增、分组一起拖动、跨页移动、撤销重做、保存重开与实时预览；从第二页进入三维编辑，修改后保存返回第二页，二维可撤销三维修改，首页内容保持。复制页保留原坐标和 zIndex，只生成新的节点/绑定/分组 ID。同页复制保持相对层叠。部分页面 API 修改不传 pageOrder 时，也按最终页面列表持久化顺序。针对复验 `m2-group-recheck.log` 共 10 项通过。

本机草稿按用户及项目隔离。同版本自动恢复；基线冲突保存在独立队列，继续编辑、保存和刷新不覆盖旧冲突草稿。恢复前显示与当前编辑器的页面/组件/绑定差异；恢复可撤销，明确放弃才清除此份冲突。`m2-pages-ui.log` 中草稿和第二页三维路径已通过，组移动原始断言将浮点差值误当精确整数，改用精度断言后上面的 10 项复验通过。

### 迁移备份事件及修复

0013 前的 Wrangler SQL 导出因不支持 `--persist-to` 失败；执行者未检查退出码就继续了迁移。这不是成功的迁移前备份。迁移已成功，API 快照比较确认三个旧画布无内容丢失。随后对隔离 SQLite 创建在线备份 `post-m2-migration.sqlite`，仅在备份副本重建旧 schema 为 `pre-m2-rollback.sqlite`；两者完整性/外键检查通过，旧副本与迁移前节点/绑定/revision 快照相符。未回滚在线测试库。

新增 `pnpm migrate:newpower` 将备份、完整性检查与迁移串在一个失败即退出的入口。`migration-guard-recheck.log` 证明备份失败时迁移回调绝不执行，成功备份保留修改前 schema；`migration-guard-live.log` 对现有隔离库实际运行通过（无待应用迁移）。后续已有库迁移统一使用该入口。根因是漏判命令失败，修复落在真实迁移入口而非仅补充提醒。


M2 综合回归 `m2-complete-regression.log`：27 项通过，1 项示例创建按设计跳过。新增定义权限与 25/100/500 组件保存/加载专项通过（`m2-scale-boundary.log`）；该轮草稿差异截图测试使用了错误 CSS 选择器，修正后 `m2-draft-diff-final.log` 1 项通过，并人工查看差异提示与第二页三维返回截图。`pnpm check` 与 web→API dry-run 构建通过（`m2-final-build.log`）。

| 组件数 | 操作 CPU p95 | 保存 | API 读回 | Chrome 挂载全部组件 |
| --- | --- | --- | --- | --- |
| 25 | 2.14 ms | 85.9 ms | 28.7 ms | 234.0 ms |
| 100 | 6.26 ms | 65.1 ms | 27.9 ms | 214.2 ms |
| 500 | 24.96 ms | 160.5 ms | 35.5 ms | 488.7 ms |

CPU 每规模 15 次；保存/加载为每规模一次的观测，后续 M7 扩大采样。环境为 Apple M5/16 GiB、Chrome 152、本地 Worker+D1；500 节点全部挂载，部分超出当前页可见边界，不是 500 个同时可见模型或生产容量证明。M1 的请求复用和离页停止在综合回归中继续通过，模型纹理/迟到加载释放进入 M3 修复。

旧版 `pnpm smoke:runtime` 的 12 项检查也通过（`m2-legacy-smoke.log`），其测试项目已按脚本清理。


## M3 模型资源生命周期检查点（2026-09-13）

起点 `e8051e4`。现有 Model3DNode 改为引用计数资源池；同一不可变 URL 共用加载/几何/纹理，SkeletonUtils 提供独立对象及骨骼实例，外观仍使用实例自有克隆材质。最后一个消费者释放原始几何、材质、纹理和 ImageBitmap；已取消的异步结果迟到时也释放。失败可重新加载，初始化失败和中途退出释放视窗资源。

纯逻辑验证并发一次加载、最后引用释放、取消迟到不会污染新请求、失败后重试；浏览器验证两个模型视窗显示独立材质、删除一个仍可显示、连续三次进入退出缓存归零、真实 ImageBitmap 关闭及断网重试恢复。`m3-resource-final.log` 7 项通过；新重试按钮被旧提示层 pointer-events 样式阻挡，修复后 `m3-resource-retry.log` 1 项通过。类型检查及构建通过（`m3-resource-build.log`，最终按钮点击样式另经浏览器复验）。第一次迟到选择回归受新增 Vite 依赖优化的页面重载影响，trace 中两次连接可见；依赖稳定后的旧二维三维联动复验通过。

这个检查点只完成资源生命周期基础；多模型同场景、资源检查清单、版本替换与绑定修复继续实现，不标记 M3 完成。


## M3 模型导入检查与对象清单检查点（2026-09-13）

起点 `56d0326`。新增 glTF Transform 4.5.0 的 WebIO/扩展解析，复用共享报告类型；新上传检查资源/默认场景三角面、顶点、内嵌纹理尺寸和字节数、默认静态姿态边界、节点结构与稳定对象UUID。资源系列与版本更新尚在后续实现，当前UUID固定于该不可变资源报告，nodeIndex只定位该文件版本。

旧模型可从三维属性面板“补充资源检查”；保留原文件和哈希，以报告JSON作CAS写入，重复或并发升级读取同一组胜出的对象ID。新接口要求编辑权限。界面显示报告和实际模型，内嵌4×8 PNG纹理经文件上传入口导入、保存、重开并实际渲染；截图已查看。

`m3-import-boundary.log` 5项通过，覆盖资源统计/GLB与GLTF、旧报告升级、纹理界面链及权限。只读复核发现GPU实例统计遗漏、FLOAT索引可误收与共享场景展开工作量缺口；修复后 `m3-inspection-review.log` 4项通过，包括实例变换后的24面和边界、浮点索引拒绝、60万GPU实例在执行顶点循环前被工作预算拒绝。随后把旧报告竞争改成同时触发两个首次升级请求，`m3-report-race.log` 1项通过。工作预算和边界精度见 m3-scenes.md。

类型检查通过（`m3-inspection-review-check.log`），最新构建通过（`m3-inspection-final-build.log`）。依赖包含未使用的NodeIO，Wrangler在依赖扫描阶段给出node:fs/path提示；实际输出 `m3-worker-build/index.js` 630,990字节，检查不存在NodeIO、node:fs或node:path，WebIO保留，且上述API在无nodejs_compat的本地Worker上真实通过。没有为此开启额外兼容权限或改变部署环境。

M3剩余：可复用场景、多模型实例、场景对象绑定、资源版本替换和修复；完整目标继续执行。


## M3 可复用场景与统一渲染检查点（2026-09-13）

起点 `945dbd3`。新增项目 schema v3、`project_scenes`、`asset_model_bindings` 与画布 sceneId；v2/旧画布自动迁移。场景/实例/对象资产映射和页面继续使用同一 CAS、编辑操作、撤销与草稿。0014 经 `pnpm migrate:newpower` 完成，工具先生成并检查实际在线 SQLite 备份。`m3-migration-readback.json` 确认三个保留项目的节点、绑定和 revision 与备份一致，外键检查为零错误。

界面通过：旧单模型转为场景，保留匹配的资产和覆盖；同一视窗添加三个实例（两个共用同一模型，另一个使用不同模型）；独立位置/材质、对象绑定、手动镜头保持、保存重开；复制页面引用同一场景，独立场景副本不互相改变。旧名称配置根据已加载对象的 glTF node index 对照资源清单转换，不靠名字猜跨版本身份。镜像缩放可编辑，零/退化缩放被拒绝；三维属性面板改为复用共享校验。

新场景的同名模型实例可分别绑定不同设备；表格选择在模型迟到加载后仍正确高亮，三维点击反向更新设备、指标和表格；设备一告警只改自己的实例材质，恢复后回到运行色。同一资产关联多个对象时全部高亮。对象清单补充默认场景成员信息；不在实际加载源场景内的对象不能成为运行绑定，旧清单补充信息时保留 UUID。

旧单模型和新场景共用 SceneViewport，实例控制器拥有独立变换/覆盖/目标表。场景实例更新不重建 renderer；镜头只在首次就绪或显式操作时取景，自动旋转按真实帧间隔运行。复核修复了失败操作后撤销未恢复、引擎初始化错误无法重试，以及旧多 primitive 子网格选择变成父组的问题；模块加载失败时重新加载页面，未保存草稿实际恢复。

资源生命周期补充检查发现：直接跨 renderer 共享 Three 资源时，保留一个视窗并反复开关另一个，会使原始几何的 dispose 监听器由 1 增到 4（`m3-retained-renderer.log`）。修复后，各视窗拥有自己的 GPU 资源副本，实例在视窗内共享；最后实例释放副本，原始模型/图像按全局 lease 释放。复验计数始终 `[0,0,0,0]`（`m3-viewport-ownership.log`）。这会为不同视窗复制几何数据，M7继续量测内存与优化；不宣称跨 WebGL context 共用 GPU 缓冲。对象清单请求也按视窗引用计数复用。

验证记录：

- `m3-scene-regression.log`：46 项通过，示例创建入口 1 项按设计跳过。
- `m3-reference-compat.log`：旧 v2 冲突草稿队列恢复/清理、源场景外对象引用拒绝，2 项通过。
- `m3-scene-final-boundary.log`：初始失败恢复、旧子网格路径、失败编辑回退、模型状态联动与性能，6 项通过。
- `m3-model-parser-final.log`：共享三维校验、镜像缩放与旧/新界面专项，5 项通过。
- `m3-scenes-final-build.log`：最终类型检查与构建；API始终dry-run。截图已检查，复核后的场景关联栏布局和复选框也已修正。

性能样本：Apple M5 / 16 GiB，Chrome152，实际驱动 `ANGLE Metal Renderer: Apple M5`。旋转镜头时各采120帧；1/10/100实例分别24/240/2400三角面，均约60FPS，p95帧间隔16.7–16.8ms。每组只请求一次模型文件和一次对象清单，视窗内共用1份几何/基础材质；加载观测约342/104/87ms。均为轻量生成模型，不能作为大模型或工业容量证明。详细原始值见 `m3-scene-final-boundary.log`。二维25/100/500组件操作CPU p95约1.62/4.17/35.85ms，仍满足100ms预算。

剩余 M3：资源版本链、模型版本替换预检、对象映射修复和可撤销替换。完整引擎目标继续，不在该检查点关闭。

旧版 `pnpm smoke:runtime` 的12项检查通过（`m3-scenes-smoke.log`），临时测试项目已清理。

## M3 版本替换完成验收（2026-09-13）

起点 `dc9eb83`。版本族与版本号、不可变文件及稳定对象映射现已实现。0015 由备份保护入口迁移；真实在线备份路径见 m3-scenes.md。相同字节按文件内部 locator 继承对象 ID，改变字节仅显式源标识自动继承；名称只给修复建议。多图元/附件纳入对象清单，旧报告升级保留已有 ID。

界面实际完成：上传后续版本但保持场景不变→取消→重新打开→新旧预览和手动修复重命名对象→应用→保存重开→撤销并再次保存。一个实例替换不会改变另一个共享旧版本的实例，资产台账和有效绑定 ID 保留。旧单模型也经过相同界面；双图元模型重排后，源标识对应的实际网格颜色与位置保持正确。预览到主视窗复用同一文件加载，最后释放仍归零。截图 `m3-version-permissions/model-versions-ui-uploadin-10f4b-d-replacement-can-be-undone/model-version-repair-preview.png` 已查看。

- `m3-version-permissions.log`：6 项通过，包含取消/撤销、完整映射拒绝、并发版本号、旧文件保留、跨项目/查看者权限和流式超限取消。
- `m3-version-repair-ui.log`：新版本修复与多图元重排界面 2 项通过；`m3-legacy-version-ui.log`：旧入口替换纹理模型 1 项通过。
- `m3-versions-regression.log`：完整 55 项通过，示例创建 1 项按设计跳过；涵盖前述专项、旧项目、草稿、页面、设备异常及资源释放。
- `m3-versions-final-check.log`、`m3-versions-build.log`：类型检查和构建通过；API 为 dry-run。原有 glTF Transform 未使用 NodeIO 的扫描提示仍存在，实际无兼容开关的 Worker 集成检查通过。
- `m3-versions-smoke.log`：旧 API smoke 12 项通过。

该轮 M5/Metal 的轻量 1/10/100 实例分别 24/240/2400 三角面，约 60 FPS，p95 16.7 ms，各 2 次资源请求、1 份视窗基础几何/材质；加载单次观测约 460/126/218 ms。25/100/500 组件保存约 17/46/97 ms，浏览器挂载约117/133/280 ms。实际规模和采样边界延续前文，未据此认定工业容量。

M3 本地技术验收通过。完整引擎目标仍 active，下一项为 M4，真实公司模型、现场接口、大模型性能和后续能力继续保持未验证/未完成状态。

## M4 交互执行链检查点（2026-09-13）

起点 `7b2f1e3`。schema v4 增加交互状态和规则；0016由在线备份保护入口迁移，三个保留项目的原字段逐表读回一致，外键零错误（`m4-migration-readback.json`）。交互配置、页面与场景共用根CAS、草稿、保存差异及撤销历史；旧v3及更早定义迁为空交互，旧画布更新保留全项目交互。

实际界面完成：添加项目状态、按钮触发器、条件及三个顺序动作→保存→撤销保存→重做保存→刷新→预览点击按钮→选择二号设备并打开详情→三维对象高亮→状态改变→隐藏图表→第二次点击因条件不满足跳过。服务器配置读回与运行前相同；返回编辑恢复图表显隐。已查看编排和运行调试截图，发现复选框被通用宽度样式拉开后修正，设备常量改为按资产名称选择。

导航浏览器验证：同一点击启动延迟动作与翻页，旧页延迟被取消；新页实际挂载后运行进入规则；项目状态保留。真实页面互相跳转继承同一事件链并在128步限制处停止，没有靠无限创建新链绕过保护。纯数据规则在没有组件绑定时仍通过现有store采集所需设备，陈旧与失联状态不满足取反数值条件，恢复后重新触发有效值。

只读复核用纯运行器重现两处问题：连续三次restart触发只取消最早已取消的run，导致执行两次；A→B→A使旧A排队事件重新有效。现已取消所有旧存活run，并引入每次页面进入代次，两项固定反例均通过。

验证记录：

- `m4-ui-fixed.log`：项目CAS/复制/悬挂引用和完整界面操作2项通过。首次测试错误地要求浏览器刷新后仍有内存撤销历史；改为保存后、刷新前实际撤销和重做，另验证重开与运行。
- `m4-lifecycle-fixed.log`：真实翻页、延迟取消/循环、独立数据需求与陈旧/失联恢复2项通过。初次样例把中文显示名用作机器ID被正确拒绝，修正为UUID后复验。
- `m4-interaction-regression.log`：完整65项通过、示例创建1项按设计跳过，包含共享运行器6项、上述API/UI专项以及M0–M3兼容/性能/资源生命周期。
- `m4-interaction-final-check.log`、`m4-interaction-build.log`：最终类型检查和构建通过；API仍为dry-run。
- `m4-interaction-smoke.log`：旧版API smoke12项通过。

运行器预算、事件/动作列表、状态作用域和迁移备份见 m4-interactions.md。轻量1/10/100实例仍约60FPS，p95帧间隔16.7–16.8ms，实际加载约330/105/119ms；原始结果保留。模型动画、镜头/路径和相应资源更新修复仍在M4后续，不把当前选择联动算作完整动画交付；完整目标继续active。

## M4 对象与镜头路径检查点（2026-09-13）

起点 `ac4d5ee`。场景增加可版本化的 motions，包含对象变换/外观和镜头位置/目标关键帧；同一共享帧循环推进，支持重复、保持末帧、停止恢复和冲突取消。编辑器能够按稳定对象选择、配置关键帧、从当前镜头取点并预览；界面与API复用校验、项目历史和根CAS。

实际操作：新增对象抬升轨道和镜头轨道→预览播放完成→停止恢复→应用/保存返回→配置按钮的播放动作与后续设备选择→保存刷新→运行中等待动画完成后才选择设备；运行位置改变而服务器配置不变。动画编辑器和运行调试截图已查看，并补充关键帧时间/XYZ标签、当前镜头取点与名称排版。颜色动画保留设备实时状态外观优先级；模型姿态和材质更新分开，状态刷新不再重置播放中的对象。

动画引用进入模型替换清单。只有动画引用、没有资产/变换/外观覆盖的对象也要求完整修复；映射后轨道目标更新，保存和撤销保留轨道/动作ID。独立复制场景会重建实例与动画ID并更新视窗播放动作。删除仍被动作引用的空动画会明确拒绝，未静默保存悬挂引用。

复核修复的三处边界：停止同时取消尚在等待视窗/模型的播放请求；纯镜头轨道等首次模型取景后才捕获基线；播放总期限包含视窗5秒、模型30秒、动画最多60秒和余量，motion.play单独为100秒。浏览器实际延迟模块/模型请求证明停止后不会迟到启动；受控时钟验证超过60秒的合法宿主播放不会被旧的60秒期限误杀（这是时间契约测试，不是长时真实帧率证明）。手动镜头接管会取消相应动画，释放后无残留播放。

- `m4-motion-references.log`：引用修复/复制/保存撤销、真实帧循环与完整UI3项通过。
- `m4-motion-loading.log`：慢加载停止、纯镜头首次取景、UI与纯播放器5项通过。
- `m4-motion-host-fixed.log`：视窗未加载前停止、长动作总预算2项通过；初次受控时钟测试未取得正确模块URL，改为读取Vite实际输出的导入URL后通过。
- `m4-motion-regression.log`：完整75项通过、示例创建1项按设计跳过。覆盖最终帧写入失败后清理、对象/镜头保持与恢复、M0–M3兼容与资源释放。
- `m4-motion-final-check.log`、`m4-motion-build.log`：类型检查与构建通过，API仅dry-run。

原生glTF动画只完成基础兼容修复与LINEAR/STEP/CUBICSPLINE实际实例测试：按需克隆轨道到各实例UUID，不更改共享原clip，颜色更新不破坏姿态，停止恢复最新配置。原生clip稳定清单、播放选择界面、资源升级修复尚待下一项；M4及完整目标均继续进行。

旧版API smoke12项通过（`m4-motion-smoke.log`），临时测试项目已清理。

## M4 原生动画与完整验收（2026-09-13）

起点 `fcbe244`。原生动画报告、片段轨道、独立mixer播放与模型版本修复已补齐，仍复用场景定义、项目操作、根CAS和视窗帧循环。报告保存片段UUID/源标识/单文件序号、时间范围及通道；同字节或显式源标识继承ID。旧报告在保留已有对象ID的前提下并发升级为同一片段清单。

实际界面完成：从原生片段创建动画→设置片段采样时间和保持末帧→预览/停止→保存→项目预览自动执行；另一个共享资源实例不串播。上传重命名且更短的新版本→明确选择片段映射→同比调整采样时间→分别播放旧/新片段（新片段可再次播放）→应用/保存→运行新版本→保存后的撤销回旧文件/旧片段。原文件字节读回不变，其他实例保持旧资源。已查看原生动画编辑和片段替换截图。

复核补充修复了非单位四元数可通过导入的问题；不同输出类型采用适当量化误差容限，立方切线不误要求单位长度。另验证骨骼关节和形态权重：目标实例实际变化，另一个克隆不变，停止恢复原值。片段的资源归属、时间上限和同实例内部姿态冲突被拒绝，实例外层路径可独立叠加。旧单模型清空选择补发与设备选择器一致的资产事件。

验证记录：

- `m4-clip-upgrade.log`：报告稳定性、旧报告并发升级、非法导入、量化旋转和旧资源检查8项通过。
- `m4-native-runtime.log`：实际原生时间线/实例隔离及报告专项4项通过；`m4-deformed-clips.log`：骨骼/形态、实例隔离和边界3项通过。
- `m4-native-ui-fixed.log`：完整原生UI/版本替换/撤销通过。初次测试在API外部改配置后仅做hash导航，仍读到编辑缓存；加显式重新加载以验证新配置，没有为测试改动产品缓存。
- `m4-native-preview-fixed.log`：旧对象替换兼容及新旧片段播放2项通过；纯对象修复保留原有简洁提示，不显示无关的零片段数量。
- `m4-native-boundary.log`：片段归属/时间冲突、旧选择清空事件、翻页取消及数据异常4项通过。
- `m4-complete-regression.log`：完整84项通过，示例创建1项按设计跳过，包含重复片段预览和此前全部M0–M4功能/权限/资源回归。
- `m4-complete-check.log`、`m4-complete-build.log`：类型检查与构建通过；API仅dry-run。`m4-complete-smoke.log`：旧版API smoke12项通过。

本机轻量1/10/100实例仍约60FPS、p95帧间隔16.7ms，每组2次资源请求；这是既定生成资源基线，不代表工业模型容量。真实公司模型、现场接口、目标硬件及M7长时规模验证继续单列。

M4本地技术验收通过。M6a继续建立独立宿主、集中采集和环境凭据；完整引擎目标保持active，后续阶段未完成。
