# NewPower 开发状态

更新时间：2026-09-13。M0/M1 已完成技术验收。当前按 NewPower_Codex_Product_Goal.md 持续推进完整引擎；M2–M8 是检查点，直到总体验收完成才关闭目标。

## 起点与范围

- 当前目录直接克隆 `NoahWorld/factory-digital-twin-platform` 的 `newpower`，起始提交 `bdf3905997f1a9301b97445a2917622b3430dc6b`；克隆前目录为空，无旧改动或本地提交。
- Owner 要求在当前项目持续推进 `newpower`；按 [研发指南](../../NewPower_Codex_Development_Start_Guide.md) 本地编辑、依赖准备、迁移、服务、验证与提交，不推送、不操作远端生产资源或其他分支。
- Node `24.18.0`，项目锁定 pnpm `10.0.0`；命令通过 `/Users/inosaki/Documents/Codex/local/tools/dev-env` 运行。工作区导入检查 0 个问题。

## 当前进度

- 仓库和指南已接手，分支适用说明已补充。
- M0 已验证：锁文件安装、web → API 顺序构建、TypeScript 检查、本地 `0001`–`0011` 全部迁移、12 项 API smoke、2 项 Chrome 浏览器验收通过。实际截图确认了静态模板及 WebGL 模型渲染。详见 [验收记录](acceptance.md)。
- 修复了根构建命令并行导致首次缺少 `web/dist`、smoke 主题样例过时的问题；增加 HTTP 503 恢复后的重新采集断言。浏览器验收通过真实登录、项目与模板保存重开、模型点击、陈旧/故障及自动恢复。
- 本地提交作者采用本机已登录 GitHub 账户 `InosakiM` 的官方 noreply 身份，仅设置当前仓库。
- M1 已实现并验证：项目级共享采集与选择；绑定配置、保存重开和删除；固定与当前选中设备；指标卡、柱状图、表格；无模型资产配置；二维三维双向联动；空值、缺失、类型错误、陈旧、故障恢复；服务端归属、权限和版本冲突校验。
- 综合验收 13 项通过，另外 1 项设备单位专项通过；示例创建测试默认跳过，避免每次回归重复创建项目。构建、TypeScript、旧版 12 项 API smoke 均通过。
- 已保留两个可操作的双设备项目，以及一个旧版单设备回归基线。本地数据与截图日志在 `/Users/inosaki/Documents/Codex/local/runtime/newpower-01a095a6/`，不在 Git 中。

## M1 实施决定

沿用资产台账、资产指标映射和 REST 标准化接口；公共组件只读标准 `metricKey`，不接触上游字段路径。新增项目级组件绑定表，节点仍只存 `dataBindingRefs`，绑定定义与画布在同一版本事务内保存。无引用的旧组件继续使用原有静态 props，运行值只在内存派生，不写回编辑配置。

绑定明确目标（指标值、对比序列、表格行）、固定/当前选择的稳定资产 ID、指标键和类型。右侧属性区配置绑定；项目工具栏增加独立于 3D 的资产与指标入口。预览共享同一需求集、缓存和资产选择，表格或设备选择器均能在纯二维页面切换资产。服务端验证项目归属、引用、目标和类型；缺值/类型错误/陈旧/失联明确显示并恢复。

## 当前入口与提交

- 交付构建与 API 同源入口：`http://127.0.0.1:8791`；开发前端 `http://127.0.0.1:5173` 也连接同一隔离 Worker。mock 监听 `127.0.0.1:8790`。
- 已在隔离 Chrome 152.0.7977.83 窗口实际登录并打开两套示例；再次验证当前构建的单位显示、实时数据和模型选择。最终截图为运行目录中的 `delivery-pure.png` 与 `delivery-combined.png`。
- 纯二维项目：`ead94aa6-63de-42b2-8930-0df588b88147`；二维三维联动：`83549ca8-cdab-4e91-b778-1bd9bb9880b2`。入口和操作见验收记录。
- M0 本地提交 `ccf3e1a`；M1 功能提交 `463c1ab`。后续交付记录为独立本地提交；当前分支仍未推送。
- 本机开发服务器需要 `NEWPOWER_POLL_WATCH=true`：原生文件通知未更新 Vite 缓存，已通过显式轮询与关闭 FsEvents 修复，并读回确认后续源码和样式变化实际更新。构建后的 8791 同源页面也已验证。

## 下一轮与边界

下一轮建议进入 M2 的一项完整操作：编辑撤销/重做与保存恢复，先沿当前增量保存和绑定事务检查最小可复用方案，再实现和验证。当前没有实现完整 M2–M8，也未宣称超越 EasyV/EasyTwin。

本轮的共享采集限定同一项目页面，最多同时发起 6 个资产采集请求；绑定可选 1–20 台设备，表格最多 6 个指标，柱状图至少 2 台设备且指标单位一致。跨浏览器集中采集、真实鉴权适配、历史数据、完整发布运行器、大模型性能与公司真实环境均属后续工作。

后续继续在当前目录、本地 `newpower` 开发和提交，不推送。已有模拟项目、测试库、运行配置与浏览器资料保留用于继续工作；不把测试库、会话、凭据或日志纳入提交。

## 持续目标接续（最新 Owner 指令）

当前基线为 `8c9325f`，四个本地提交全部保留；工作区新增 Owner 提供的产品目标文件。三个服务仍在运行，隔离库读回首页 revision 1、5 个节点、4 个绑定。

第一项 M2 正在实现：提取现有纯画布校验到共享模块，建立受校验的编辑操作，支持复制绑定组件、删除、撤销重做、保存重开与正确预览。随后继续多选分组、多页面、版本迁移和草稿恢复，不等待再次指示。多页面保留项目级 revision 和绑定存储，引用回收必须覆盖全部页面。

M2 当前进展：共享校验和编辑命令已通过真实复制/删除/撤销重做/保存重开操作链。复核发现的命令与引用边界已修复；性能独立复验 500 组件 p95 约 41 ms。接着实现多页面、分组和草稿恢复，完整引擎目标保持 active。


## M2 完成与 M3 接续（2026-09-13）

共享项目 schema v2、多页面和稳定 ID、分组/跨页移动、完整项目历史、二维三维共用编辑状态、保存与冲突草稿恢复已完成本地技术验收。27 项综合回归通过，另有新定义权限和25/100/500节点持久化量测通过，草稿差异界面专项通过。构建和类型检查通过；具体日志、失败修复与迁移备份事件见 acceptance.md。

下一项 M3：先完善模型资源检查和加载/释放，随后实现同一场景中多个模型实例、共享场景引用、稳定对象映射与版本替换修复。继续复用现有三维渲染和项目级命令/CAS，不另建孤立的场景编辑状态。M2 是检查点，完整引擎目标保持 active，后续 M3–M8 尚未完成。

M3 首项资源生命周期已完成本地检查点：并发共享、对象/外观独立、最后释放、取消迟到释放、失败重试和旧联动回归通过（7+1项）。下一步补模型检查报告与稳定对象清单，接可复用多模型场景；详见 m3-scenes.md。

M3资源检查报告与旧报告升级已验证：GLB/GLTF统计、纹理界面上传保存重开、稳定UUID、首次升级CAS竞争、权限、GPU实例边界及非法输入检查通过。接着实现项目级可复用场景和多个独立模型实例，保持同一项目CAS、历史和运行解释规则。


M3 可复用场景检查点：schema v3/0014 已迁移并完成旧数据读回；场景/实例/对象映射与页面统一CAS和历史。新旧模型共用renderer，三实例组合、跨页共享/独立复制、同名实例设备联动、错误恢复与资源释放已验证。综合46项通过，追加兼容/边界专项通过；1/10/100轻量实例约60FPS。下一步实现资源版本链及可撤销的替换与映射修复，M3和完整目标继续进行。

## M3 完成与 M4 接续（2026-09-13）

资源版本族与不可变文件、并发版本分配、新旧预览和完整映射修复已完成。替换只影响所选实例，取消、保存重开及保存后的撤销均通过；旧单模型采用相同修复入口，多图元重排仍保持正确颜色、位置和资产映射。0015 已在实际备份成功后迁移。最新综合回归 55 项通过、示例创建 1 项按设计跳过；类型检查、构建和旧 smoke 12 项通过，日志见 acceptance.md。只读源码复核未发现新的具体缺陷。

下一项 M4：为现有选择、数据和模型运行层建立共用事件/条件/动作/状态及受校验配置，接入二维三维交互、异步取消、动画与调试。先交付可保存、撤销、重开并真实执行的交互链，再扩展动画和复用。新增动作目标必须一起进入复制与模型替换引用修复，不能留下隐藏的悬挂引用。M4–M8 与总体验收继续进行。

M4 首项交互链已通过本地技术检查点：schema v4 与0016、共享条件/动作执行器、可视化编排、状态变量、资产选择/显隐/翻页/等待/自定义事件和运行调试已接入。按钮实际驱动设备选择与三维高亮，规则复制、保存重开和保存后撤销通过；翻页取消与循环保护、数据陈旧/失联恢复通过。复核发现的三连触发漏取消、同页旧事件复活已修复。完整回归65项通过、示例1项跳过，check/build/smoke12通过。下一步继续模型动画、镜头与路径及其复制/资源替换引用修复，M4尚未完成。

M4 关键帧与路径已接入：场景拥有可复用对象/镜头轨道，界面可编辑并按当前视角取点；播放/停止使用现有帧循环，接顺序交互与取消。复制场景及模型替换同步维护轨道/播放引用，移除仍被动作引用的动画会明确阻断。原生glTF轨道已完成克隆实例重定向和播放姿态/材质隔离基础修复；原生clip稳定清单、用户播放入口与版本修复仍在下一项。

## M4 完成与 M6a 接续（2026-09-13）

事件/条件/动作/状态、关键帧和镜头路径、原生glTF动画与可视化编排调试均已完成本地技术验收。原生片段具有稳定清单与源标识，旧报告并发升级、模型版本片段映射、时间缩放、新旧片段预览、保存撤销通过；实例、骨骼关节与形态权重独立，停止释放恢复配置。最终84项回归通过、1项示例创建按设计跳过，check/build通过；完整证据见acceptance.md。

下一项M6a采用本地Node宿主复用现有API、SQLite/ObjectBucket适配和可移植采集核心。先验证独立宿主的会话/项目/CAS/资源，再接多客户端集中REST采集、生命周期、环境凭据与WebSocket。这个宿主是M5独立交付的必要前置，不替代M5发布/打包验收。具体顺序见m6a-collection.md；M6a、M5、M6b、M7、M8和总体验收继续进行。

M6a独立宿主基础已验证：复制构建目录后仅用Node运行同源前端/API，SQLite迁移/备份/事务、文件资源、权限、保存重启与Chrome双设备联动通过。取消和服务关闭的异步边界已修复；15项最终专项、三包check/build、Worker smoke12通过。健康状态仍明确 `per-request`；下一项实现按数据源集中采集和多客户端生命周期，M6a未完成。

M6a集中REST首项正在回归：Node按源共享采集与SSE，前端按宿主能力复用原设备连接状态。两个独立Chrome客户端和两个资产共用一次源采集，关闭一个不断流、最后退出停止；503、陈旧、恢复已实测通过。配置加载交错、迟到响应、绑定失效保留身份/旧值和初始超限释放反例已补。持续采集、环境凭据和WS尚未完成。

集中REST首项回归已收齐：完整100项中的98通过、1示例跳过；唯一镜头测试因固定100帧未等阻尼稳定而失败，改为测得稳定后保持原精度校验，场景专项通过。最终传输/宿主/双客户端/场景10项复验、check/build和Worker smoke12通过。终态重试与畸形live快照拒绝已补。接续服务器环境逻辑引用、凭据隔离与持续采集，再接WS。

M6a环境端点/凭据已通过本地检查点：逻辑引用界面保存、真实HTTP鉴权、标准即时/SSE输出、私有回显拒绝、查看者隐藏和重启换环境通过；项目源配置保持不变。共用URL校验，凭据绑定项目/端点；Node超限请求停止读取并正确发出413。最终5项、前置9项关闭/环境检查、check/build、Worker smoke12通过。下一项显式持续采集及服务启动恢复，然后真实WS上游。

M6a持续采集已接入并完成首轮实际检查：数据源模式由表单保存，Node重启恢复；零查看订阅仍持续采样，改回按需停止；重启遇503时新epoch/空采样，恢复后重新采样。容量257源的合成边界明确显示一个失败，释放后恢复调度。新增字段撑长面板时发现保存按钮出界，已修复固定面板内部滚动；逻辑环境原界面回归也通过。正在收齐相关回归，随后真实WS上游。

## M6a 完成与 M5 接续（2026-09-13）

独立Node宿主、SQLite/文件适配、集中REST/WS、按需/持续生命周期、环境逻辑引用和凭据隔离已完成本机技术验收。实际双独立Chrome共享源连接，断连/陈旧/订阅恢复、最后释放和持续重启通过。完整113项通过、示例1项按设计跳过，check/build与旧Worker smoke12通过；协议边界和资源关闭有专项，详见acceptance.md。

下一项M5先冻结完整运行快照并实现固定版本运行、发布/激活/回滚，之后让导入导出复用这份快照。必须覆盖画布之外的资产、源和指标映射，并以完整配置修订做CAS；采集不能继续读取草稿配置。资源文件真实验证和私有环境分离随快照建立，具体闭环见m5-publishing.md。M5、M6b、M7、M8与总体验收尚未完成。

M5首项正在实现：0017完整配置修订与发布指针已迁移；新迁移先在线备份，首次被正在运行的Worker锁阻止且未应用，停掉该Worker后重新备份并成功迁移，按原配置恢复。三保留项目15段读回完全相同、FK0。完整快照、文件实际SHA256验证、创建/激活CAS、固定版本定义/catalog/流、发布与回滚面板已接入；当前UI/API专项通过，尚未完成M5导入导出。

新触发器暴露D1 meta.changes含触发器更新的差异，造成成功写入却误报409；适配层改为同事务读取SQLite changes()，Node原生保持准确changes。旧Worker权限/绑定/旧模型发布复验通过。旧名称使用同版Three加载器的实际运行名/locator清单，兼容sanitize及多primitive。停机取消激活检查且发布指针不写入的实际验证通过；后续仍需覆盖发布持续源、更多场景/动画和全回归后提交检查点。

M5冻结发布检查点已验证：创建/激活/回滚UI、完整配置CAS、固定版本页面/数据/模型报告、原生动画/翻页和持续源重启隔离通过。全回归120项通过1示例跳过，最终名称歧义/引用/Worker/UI专项5项通过，check/build/锁文件安装/smoke12通过。旧名称歧义明确提示稳定转换，D1触发器行数差异已在适配层修复。下一项项目包导入导出与干净环境交付；M5尚未完成。

M5项目包首项正在回归：ZIP导出外部化端点，Node以中央目录/实际解压长度/CRC/SHA及模型报告验证后安装；新项目与已有项目版本安装、身份复用和冲突拒绝通过。真实UI已验证：干净目标安装、配置私有端点、停止原服务后二维三维运行、恢复可编辑草稿、安装新版与回滚。0018已先备份、停原Worker后迁移并恢复；三示例15段读回不变、FK0。

导入资源写入前持久化journal；实际SIGKILL后重启清理未提交对象并可重试。运行目录由独立SQLite原生锁限定单宿主，进程死亡自动释放；安装最终事务及已消费检查重试都复核当前项目权限。Buffer切片越界读取已用精确字节复制修复并加偏移反例。项目包代码尚未提交，后续完成兼容/负面回归和可交付运行包；M5总体验收未完成。

项目包完整回归132项通过、1示例按设计跳过。其后补充运行程序ZIP，实际下载→解压干净目录→启动脚本生成私有环境→初始化管理员→导入项目→停止原宿主后运行已通过；程序包含真实依赖许可，不含数据库/会话/实际凭据。HTTPS反向代理的Secure Cookie、同源模型与SSE/退出释放也已实测。本机没有Docker命令，Windows启动脚本未在Windows执行；不将这些环境写成已验证。仍在完成最终针对性复核、稳定验证与交付记录。

## M5 完成与 M6b 接续（2026-09-13）

冻结发布、完整资源项目包、独立程序ZIP、干净目录启动、私有环境配置、恢复编辑、升级/回滚和HTTPS同源适配已完成本机技术验收。完整132项通过1示例跳过，后续交付/恢复/权限10项及原生动画/图片导入浏览器专项通过；check/build/锁文件安装和Worker smoke12通过。0018备份与三保留示例读回一致。Windows/Docker及真实现场未验证，详见acceptance.md与project-delivery.md。

下一项M6b从无人查看也生效的历史与告警链开始：当前collector仅在客户端更新中标准化资产，须在源采样入口建立有配置身份的标准化/持久化处理，再接告警发生/恢复及查询界面。草稿和冻结版本严格分开，多查看者不重复记录，失联/陈旧不假恢复。随后继续WS历史、转换、MQTT/只读SQL适配与诊断，具体见m6b-telemetry.md；完整引擎目标保持active。
