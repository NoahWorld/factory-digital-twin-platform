# 项目集成验收 · 2026-09-08

验收对象：`builtin:aqua-helix-hd-v1`，打包 SHA-256 见 `package-report.json`。使用生产前端构建和隔离的本地 Worker/D1，无 R2 绑定；未更改真实项目数据。

- `pnpm check`：前端和 API TypeScript 检查通过。
- Web Vite 构建与 API Wrangler dry-run 构建通过。Three.js 懒加载块仍有超过 500 kB 的构建提示，没有据此承诺设备帧率。
- `pnpm test:models`：文件哈希、独立节点、内嵌资源、420 条动画通道循环、共享 schema、旧配置迁移、非法参数与未知资源拒绝通过。
- `pnpm test:ornaments`：现有组件、配置契约和数据库往返回归通过。
- `pnpm smoke:models`：需要认证的模型列表、匿名拒绝、无效 ID/参数拒绝、配置保存往返、无 R2 加载内置 GLB、上传仍显式报告存储缺失通过。
- 浏览器：从空 3D 组件点击使用样例；透明/实体/内部检视；暂停与恢复、水流显隐；连续 0%/100% 拆解；直接点选电机线圈；节点树选择电机组件及独立显隐；恢复原值；保存后重新进入与返回画布；项目运行预览均通过。最终预览的浏览器错误/警告日志为空。

连续拆解验收中修复了父级中心偏移未更新就测量包围盒的问题。拆解只改变组件父节点，避免重置暂停动画的当前姿态。前端采用同一模型组件渲染编辑器、画布和运行预览。

此记录验证模型与编辑器集成；验收项目没有绑定设备资产或实时数据，运行页会如实显示已有的未绑定提示。

## 2026-09-08：可选运行控制面板

- 新增 `showControlPanel`；前后端 true/false 一致，旧配置迁移为 false，字符串、数字、null 和对象拒绝保存。
- `pnpm check`、`pnpm test:models`、前端生产构建通过；隔离 Worker 的 `pnpm smoke:models` 验证开关保存回读及非法值 400。
- 浏览器在本地隔离项目 `3818b2b4-f1bc-42e4-9811-917e16fce6a7` 验证：3D 编辑器开启并保存，预览出现面板；隐藏外壳、暂停动画、0.5× 速度、关闭水流、100% 拆解均生效。
- 恢复预设还原全部控件；收起面板可隐藏控件；刷新重新使用透明外壳、1× 播放、水流开启、0% 拆解，画布版本未被临时操作改变。
- 2D 属性栏关闭并保存，预览面板消失；2D 编辑画布本身没有运行交互面板。最终样式已截图检查，浏览器无 error/warn。测试项目没有设备绑定，现有 REST 采集提示如实保留。
