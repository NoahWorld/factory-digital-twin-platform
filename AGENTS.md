# 工厂数字孪生交付平台：项目协作规则

本文件只约束当前仓库。通用工程规则以全局 `AGENTS.md` 为准；两者冲突时，优先遵守更严格的规则。

## 产品定位

- 产品是面向交付人员的工厂数字孪生项目生成器，不是通用低代码/SaaS 大屏产品。
- 前端展示名称的唯一配置入口是 `VITE_PRODUCT_NAME`；未配置时由 `apps/web/product.config.ts` 使用默认值 `Kingdom 3D vision`。宣传页、工作台导航、浏览器标题与 SEO 元信息必须统一读取该配置；显式配置空字符串属于错误，必须让启动或构建失败，不能静默回退。
- 目标是以行业模板、标准 glTF 2.x 模型、资产台账和数据契约，快速交付两类一等项目：保留原有 **2D + 3D 组件组合大屏**，并新增可独立搭建、漫游和展示的 **大型 3D 场景项目**。
- 首期运行态是只读大屏；配置能力只向交付人员开放。
- 公开产品宣传页只保留 3D 工业交付展示版：`#/` 是默认入口，`#/industrial` 保留为兼容入口。首屏以可辨认、可交互的工厂模型解释场景、资产与数据联动；演示仅使用本地内置模型与明确标注的模拟数据，复用现有场景运行时，不得请求客户资源或业务 API。“进入平台”统一跳转到受身份验证保护的 `#/projects`。宣传页不得发起项目、用户或客户数据请求，也不得把尚未实现的能力、性能数字或数据连接状态写成既成事实。
- 宣传页精密取放案例使用独立的本地公开 GLB 清单与严格资源解析器，复用共享运行层的可选原生动画控制，并显式启用循环重播；不改客户母版，不生成替代运动轨迹，界面不显示进度条，保留暂停与查看控制。来源、重建与验证见 `docs/precision-motion-demo.md`，资源变更须运行 `pnpm test:precision-demo`，有本地母版时追加 `--verify-source`。
- 当前详细方案的单一事实源为 [工厂数字孪生平台-启动准备与首期方案.md](./工厂数字孪生平台-启动准备与首期方案.md)。产品范围发生变化时，必须同步更新该文档和本文件。
- 2026-09-15 用户确认后续交付目标：客户提供模型和业务流程，交付人员在 dtwin 内配置部件、动作、工序、材质灯光、特效、镜头和看板，发布达到凯德单体项目效果的成品。静态部件的动作制作与已有动画的编排均属于目标；不能把接收已完成动画当作全部目标。具体范围、实施顺序和验收见详细方案第 11 节，新增能力在通过验收前均为待实现。

## 首期范围与非目标

首期要支持：

- GLB/GLTF 模型导入、校验、浏览、对象选中/高亮、区域分组和镜头定位。
- 新建项目时显式选择 2D 或 3D：2D 继续使用原有画布与模板；3D 使用独立场景、模型积木、资产绑定、预览与后续发布工作区。
- 预制工厂模板：总览、产线、设备详情、能耗、告警。
- REST 轮询与 WebSocket 数据接入，以及 2D/3D 基于同一资产的联动。

首期明确不做：

- 无限画布、组件市场、用户脚本、在线协同编辑或“复刻 EasyV”。
- 浏览器直连 PLC、OPC UA、MQTT 或客户内网工业设备。
- 原始 BIM/CAD 直接渲染、GIS 城市级场景、倾斜摄影、云渲染和 Unreal。
- 为个别客户的模型或接口写进不可复用的特例逻辑。

## 不可破坏的领域约束

### 资产是关联中心

- `assetId` 是模型、资产台账、数据、告警、2D 组件和 3D 状态的唯一稳定关联键。
- `assets.id` 只作为数据库记录主键；`assets.asset_key` 通过 API 暴露为 `assetId`，是客户现场与跨模块共享的稳定业务编号，两者不得混用。
- `modelNode` 仅用于 glTF 模型节点映射，不能替代 `assetId`；模型节点名必须稳定且唯一。
- 当前首期在单个项目内以唯一 `modelNode` 建立资产映射；解除模型节点绑定只清空 `modelNode`，必须保留资产台账记录及稳定 `assetId`。
- 编辑器节点树直接来自已加载的 glTF 场景；子节点索引路径只用于当前编辑会话中的定位与选择，持久化的资产映射不得依赖可能随重新导出变化的索引路径。
- 模型视窗的对象点击与节点树选择必须双向同步；编辑态包围框高亮只属于当前会话，禁止写入画布 JSON，也不得通过修改模型材质实现。
- 模型节点位置、旋转和缩放只保存相对原始模型的稀疏覆盖，并以经过唯一性检查的模型节点名为键；未命名或重名节点不得保存覆盖。单个 3D 组件最多保存 100 个节点变换，禁止把完整场景树复制进画布 JSON。
- 模型节点颜色、透明度和显隐同样只保存以唯一节点名为键的稀疏覆盖，单个 3D 组件最多保存 100 个节点外观。运行时必须按被覆盖对象克隆材质，禁止直接修改共享原始材质而污染其他对象；覆盖重算或组件销毁时必须恢复原材质并释放克隆。
- 3D 场景的纯色/透明背景、环境光、主光源、初始视角、FOV 和整机显示比例属于轻量画布配置；整机显示比例必须在编辑器与预览中一致还原，视口滚轮缩放仅作为未保存的临时查看。HDR 环境贴图、天空盒与后续贴图文件必须走资源 ID，禁止把二进制或 data URL 写入节点 JSON。
- 3D 场景和 `model-3d` 组件的 `preventBottomView` 默认开启，作为正式配置保存；仅旧数据缺失时补 `true`，显式 `false` 必须保留，`null` 与非布尔值必须报错。开启时 OrbitControls 限制极角不超过 90° 并使用地面平移，关闭恢复自由查看；所有相机预设保持 Y-up，切换限制不得重置用户缩放与目标。编辑、预览、内嵌场景与单模型共用限制逻辑，不增加渲染循环。Java 默认值读取生成契约，历史 Worker 依赖迁移 `0021_scene_bottom_view.sql`。
- 3D 组件是一个场景容器，可显式保存最多 32 个 `modelInstances`；每个实例只保存稳定实例 ID、模型资源 ID、名称、显隐与整体位置/旋转/缩放。`resourceRefs` 必须按实例首次出现顺序保存不重复的资源 ID，并与实例引用集合完全一致；首个实例是主模型，当前节点树、节点级覆盖和资产映射只作用于主模型。缺少 `modelInstances` 的旧单模型节点按首个 `resourceRefs` 兼容读取，禁止迁移时复制模型文件或完整场景树。
- 独立 3D 项目的场景数据不得复制进 2D 画布节点 JSON。场景继续使用 `standalone_3d_scenes` 与按实例规范化的 `standalone_3d_instances` 增量保存；2D 画布的 `scene-3d` 节点只保存有权限访问的 3D 项目 ID 和交互开关，在编辑与预览时读取该项目的已保存场景。实例除名称、用途、显隐和变换外，还可保存动画启停/速度与颜色/透明度。当前硬预算为 128 个实例、24 个唯一模型、150 MB 唯一模型资源、6000 个估算网格实例、开启动画时 24 个动画实例、单次补丁 100 个实例变更。动画实例预算只统计场景全局动画开启且实例动画开启的资源。预算由前端预检并由 API 再次强制校验，不能只依赖按钮禁用。
- 独立 3D 编辑器左侧“图层”必须完整映射当前场景实例，并与画布拾取使用同一个选中实例 ID；编辑态点击图层或模型都应同步选中并显示 `BoxHelper` 与 XYZ 操作轴。预览与融合看板运行态不得显示编辑包围框或操作轴，业务设备选择使用贴地脉冲环表达。右侧属性按基础信息、变换、外观和动画分区；场景属性按项目关联、背景、灯光、相机和全局动态分区。实例颜色/透明度属于实例级外观，运行时必须克隆该实例材质，恢复默认或销毁时释放克隆，禁止污染共享模型资源。对应数据库扩展为 `0019_standalone_3d_instance_presentation.sql`。
- 资产映射中的重复 `assetId`、未匹配节点、重复匹配和缺失关键字段是发布阻断错误，不能静默跳过或默认绑定。
- 资产指标映射按行保存在 `asset_data_bindings`，通过资产记录主键关联稳定 `assetId`，不得塞入画布节点 JSON。每条映射必须引用同项目数据源，并显式保存唯一 `metricKey`、以 `$` 开头的源字段路径、值类型、单位和数据陈旧时间。
- 客户原始 `dataKey` 只能存在于数据源映射层，前端组件不可直接依赖客户原始字段路径。

### 数据网关是安全边界

- 本地前端地址（含端口）必须与 `deploy/local/.env` 的 `ALLOWED_ORIGINS` 精确一致。Vite 因端口占用切到其他端口后，登录等写请求会被 Java 拒绝；先确认实际来源，经授权调整本地白名单并重新创建 API 容器使环境变量生效，禁止放宽为任意来源、伪造 Origin 或关闭校验。
- 浏览器只能访问项目配置和经过标准化、授权后的数据；客户 API 密钥、Cookie、令牌、内网地址不得下发到浏览器或提交到 Git。
- 统一数据事件至少包含 `assetId`、`timestamp` 与 `values`。字段定义必须有类型、单位、枚举/阈值和数据陈旧时间。
- REST 必须配置超时、频率限制、失败上下文和陈旧数据标记；WebSocket 必须有连接状态、心跳、订阅恢复和有界退避重连。
- 项目级数据源保存经过校验的 REST 轮询或 WebSocket 连接元数据；REST 可保存源数据时间戳的 JSON 路径，用于识别 HTTP 正常但数据停止更新。URL 禁止内嵌用户名、密码或常见敏感查询参数，认证只能保存服务端 `credentialRef`。既有 Worker 仅有用于 P1 本地竖切验证的按资产 REST 即时采集端点；它必须由 `RUNTIME_POLLING_ENABLED=true` 显式开启，并且只允许访问 `RUNTIME_ALLOWED_HOSTS` 白名单中的主机。Worker 的 WebSocket 执行、凭据解析、生产级调度、限流和集中状态缓存尚未实现。新增 Java 实现有独立 REST collector、Valkey 最新态/短期增量流和浏览器 WebSocket，采集使用 `RUNTIME_ALLOWED_ORIGINS` 明确允许的来源；上游 WebSocket 与凭据解析仍未实现。现有前端尚未改为项目级 WS 订阅，界面不得把待实现能力宣称为可用。
- 本地 REST 竖切对每次请求执行超时、拒绝重定向、256 KiB 响应体上限和严格 JSON 字段/类型校验；配置源时间戳路径后，以同一数据源映射中最短的 `staleAfterSeconds` 判定陈旧，超过 5 分钟的未来时间戳视为上游时钟错误。已保存 REST 数据源的连接测试仅允许项目编辑者执行，必须复用相同的运行开关、主机白名单、超时、重定向与响应体限制；字段发现只返回最多 200 个标量路径，递归深度最多 8 层、每个数组最多检查前 10 项，禁止把整份上游 JSON 持久化或写入画布。采集和测试的成功与失败必须记录 `requestId`、项目、耗时及错误码；按资产采集还必须记录资产上下文。运行态仅保留最后一次成功快照用于诊断，请求失败必须立即标记“失联”，时间戳过期必须标记“数据陈旧”，并明确说明旧值不是当前实时值。
- 真实数据错误、字段映射失败和设备失联必须被明确展示和记录；不得用假数据、空数据或“正常状态”掩盖。
- Mock/Excel 数据只用于演示和联调，运行界面必须明确标为模拟数据。

### 2D 与 3D 的配置关系

- `projects.project_type` 是项目工作区路由的唯一事实源，只允许 `2d` 或 `3d`。既有项目迁移后保持 `2d`；模板中心按 2D / 3D 区分并创建相应类型的新项目。2D 项目沿用原有画布、3D 组件和预览代码，3D 项目不得创建或伪造 2D 画布。
- 独立 3D 项目可以显式关联一个用户有权限访问的 2D 项目，但两边仍分别版本化、分别保存，不复制画布或场景配置。两边通过稳定 `assetId` 绑定同一业务设备，通过统一交互事件传递选择、聚焦与数据更新；事件必须同时声明 `originProjectId` 与 `targetProjectId`，避免多个项目页签之间串扰。同窗口使用 `CustomEvent`，跨页签使用 `BroadcastChannel`，重新打开页面时通过 URL 的 `asset` 参数恢复选择。浏览器事件只用于交互加速，服务端资产和数据 API 才是数据事实源。
- 独立 3D 预览必须读取关联 2D 项目的已保存画布，并以透明叠加方式显示真实 2D 组件；复用 `CanvasSurface`，排除 `model-3d` / `scene-3d` 节点以免重复渲染。只让实际控件与配置点击动作的组件接收指针，空白区域留给 3D 操作；全屏根包含 3D、2D 和消息/详情，不以自动生成的设备看板替代用户布局。两边仍分别保存、分别修订。
- 2D 节点的 `interaction` 与独立 3D 实例的 `clickActions` 共用 `shared/twin-actions.ts` 白名单，最多 8 项，支持文字消息、业务设备详情、2D 组件显隐、模型聚焦和纯文本更新。旧实例缺失点击配置保留资产选择语义，显式 `[]` 关闭默认动作。运行态临时状态不写回配置；保存时校验最终文档及关联目标权限、运行时整批校验后执行，错误必须可见。Worker 使用正式迁移 `0022_twin_actions.sql`，Java JSONB 沿用生成契约。完整边界与验证见 [联动交互](./docs/linked-2d-3d-interactions.md)。
- 新版交互事件以项目 ID 定向，经同窗口 CustomEvent / 同源 BroadcastChannel 传递，使用相关 ID、5 分钟时效与有界去重；接收端不得重播，且不能绕过服务端权限。模型聚焦复用现有相机，跨项目目标需已打开对应预览或嵌入当前画布，不宣称后台已打开或执行。2D 没有内嵌场景时仅在明确选择设备后轮询该资产，禁止静态看板空转轮询；浏览器直连仍受单页 50 台上限约束。
- 未关联 2D 项目、未绑定业务资产属于未启用可选设备联动，独立 3D 预览与 2D 内嵌场景不得为此显示警告、点击提示或虚假的“正在连接”；没有绑定设备时不显示自动生成的设备看板。配置引导留在编辑器属性面板。真实资产加载失败、已配置引用失效、采集超限、失联和陈旧状态仍须明确展示；不得通过隐藏全部状态组件掩盖错误。运行 `pnpm test:runtime-ui` 验证无连接、加载、正常连接和异常状态的显示边界。
- 2D 画布中的 `model-3d` 继续承担轻量单模型/小组合能力；`scene-3d` 通过项目 ID 引用已搭建的独立 3D 车间，并在同一画布中与 2D 节点自由排版。引用节点与源场景分别版本化、分别保存；源场景模型、实例、资源和资产绑定仍以独立场景表为唯一事实源，画布不得复制或局部改写它们。
- 首期 2D 使用有边界的 1920 × 1080 交付画布，只允许白名单组件拖拽、吸附、四角缩放、属性与静态数据配置和预览；当前白名单为 3D 模型、3D 场景、设备数据、折线图、柱状图、面积图、饼图、环形图、雷达图、指标卡、环形进度、进度排行、状态矩阵、数据排名、实时告警、业务表格、事件时间线、矩形、圆形、大屏标题、背景点缀、雷达扫描、数据流光、电路脉冲、能量核心、工业流线、网格扫描、时间日期、标题、小卡片背景、小图标背景、科技面板、纯文本、文字超链接、图片、轮播图、按钮、全屏切换、Switch、多选框、单选框和下拉菜单，不扩展为无限画布、组件市场或用户脚本平台。科技面板是纯视觉背景节点，允许其他节点在其上方排版，但当前不承担分组、嵌套、联动或批量移动语义。
- 项目列表中，点击卡片封面进入画布编辑；卡片操作区的眼睛按钮必须直接进入该项目预览路由，不得先经过编辑页。
- 新建独立 3D 项目的空场景不提供“搭建示例车间”按钮，画布只显示保存的背景和真实地面网格，不覆盖空态文字。添加引导仅放在左侧图层/模型库。没有模型时也须初始化可观察网格的相机，首个模型加入后仍正常取景；只读、预览和封面复用同一场景，加载与错误提示不受影响。
- 独立 3D 模型库使用内置模型已有的几何派生静态缩略图，接近可视区才加载，固定占位尺寸并异步解码；列表本身不得下载 GLB 或创建 WebGL renderer。点击缩略图才挂载一个真实模型预览，默认不播放动画，关闭后释放资源并恢复焦点。上传模型尚无服务端缩略图生成流程，须明确标为“暂无缩略图”并允许按需预览，禁止用概念图伪装。后续缓存缩略图必须以模型内容哈希/渲染版本区分并沿用项目授权，不可公开客户模型。
- 组件最小尺寸按类型约束：3D 模型为 360 × 240，3D 场景为 480 × 320，设备数据为 300 × 260；折线图、柱状图和面积图为 240 × 160，饼图与环形图为 240 × 200，雷达图为 260 × 220；矩形为 240 × 160、圆形为 240 × 240；指标卡为 200 × 120、环形进度为 240 × 220、进度排行为 280 × 220、状态矩阵为 300 × 200、数据排名为 300 × 220、实时告警为 320 × 220、业务表格为 360 × 220、事件时间线为 320 × 240；大屏标题为 360 × 72、背景点缀为 200 × 72、雷达扫描和能量核心为 160 × 160、数据流光为 200 × 72、电路脉冲和网格扫描为 240 × 120、工业流线为 200 × 64、时间日期为 220 × 72、标题为 160 × 48、小卡片背景为 160 × 100、小图标背景为 64 × 64、科技面板为 260 × 180、纯文本和文字超链接为 160 × 48、图片为 160 × 100、轮播图为 240 × 160、按钮与全屏切换为 120 × 48、Switch 为 160 × 48、多选框和单选框为 200 × 96、下拉菜单为 180 × 64；圆形、雷达扫描和能量核心锁定 1:1，前端缩放和后端校验必须使用同一组约束。
- 图表静态数据每个组件最多 32 个数据点，雷达图至少 3 个维度，饼图和环形图只接受非负且总和大于 0 的数值；数据排名最多 12 项，实时告警和事件时间线最多 20 项，业务表格最多 8 列、20 行。以上限制必须在属性面板和 API 校验层同时执行，实时数据接入后仍只在节点中保存数据绑定 ID，不把连续数据流写入画布 JSON。
- 内置大屏模板是前端维护的静态预设，只负责实例化上述同一套白名单节点，不维护第二份持久化 schema。工作台 `#/templates` 只通过模板创建新草稿项目，禁止选择或静默覆盖已有项目；画布工具栏保留就近入口，只替换当前项目的本地编辑布局。两种入口都必须由交付人员显式保存，所有模板初始指标必须显示“示例数据”，不得伪装成已接入现场数据。
- 3D 场景模板注册于 `apps/web/src/scene/scene-templates.ts`，示例车间复用 `shared/workshop-layout.ts` 的实例与设置，禁止维护第二套布局。模板中心创建新 3D 项目；编辑器工具栏“模板”在显式确认后仅替换当前草稿，保留项目身份和 2D 关联，但不自动绑定业务资产或保存。模板 URL 仅用于修订版 0、无实例、无关联且未改动的新空项目，禁止重放覆盖已有场景；套用前校验编辑权限、资源完整性、渲染预算及包含删除项的实际补丁预算。模板卡只懒加载真实模型静态缩略图，并标为模板所含模型，不把模型拼图当作项目截图；不加载 GLB 或创建 WebGL。运行 `pnpm test:scene-templates`、`pnpm test:standalone-3d` 和前端 check/build 验证。
- 当前模板库至少覆盖设备检修、装备保障、工业制造、能源电力、园区运营、仓储物流、水务环保和数据中心。模板中心与宣传页的模板缩略图必须直接读取同一份 `CanvasNode[]` 布局数据生成，禁止另画一套与真实画布脱节的 CSS 占位图；新增行业入口时必须同时提供可创建、可保存、可继续编辑的完整 1920 × 1080 模板。
- 画布主题统一保存主题预设、背景纹理、字体风格、辉光强度、统一面板圆角，以及画布背景、组件背景、文字、强调和边框五个基础色。内置预设至少包含深海科技蓝、钢铁警示橙、能源监控绿、指挥中心金和明亮工业白；自定义主题仍只保存同一份正式配置。应用主题时必须显式更新除 `model-3d` 和 `scene-3d` 外所有节点的颜色属性，新建可设色节点继承当前主题；不得改写 3D 模型或引用场景的背景、灯光、材质及其他配置。主题与节点变更使用同一画布版本并由交付人员显式保存，禁止维护只存在于浏览器或预览态的第二份主题状态。
- 图表调整尺寸时必须按当前容器尺寸重新计算绘图区、标签密度和图元坐标，禁止整体非等比拉伸 SVG；文字点缀按容器重新排版，禁止通过 transform 拉伸字体；圆形和小图标背景必须锁定 1:1 宽高比。
- 组件内部只展示用户配置的内容、业务数据和必要的真实状态；编辑态、预览态及缩略图都不得叠加“可拖拽”“拖动”“点击配置”等操作提示、组件类型小字或资源数量。未绑定图片、轮播或模型时不在组件内显示导入引导，配置说明只放在组件库或属性面板。选中边框、缩放手柄和无障碍标签保留；加载失败、配置错误、失联/陈旧状态及可配置的示例数据标识不得被隐藏。
- 同一页面中的时间日期组件必须共享一个时钟调度器，不得为每个组件单独创建定时器；组件卸载且无订阅者后必须停止计时器。
- 新增 `card-title` 卡片标题和 `vector-icon` 矢量图标，最低尺寸分别为 120 × 32、24 × 24；独立图标锁定 1:1。标题支持简洁、细线、渐变色带、工业切角四种样式，文字与图标独立配置大小、颜色、粗细，拉伸容器不得改变字形。新节点校验和尺寸使用 `shared/canvas-ornaments.ts`，前后端复用；仅保存图标 ID 和轻量样式，不接受外部资源、任意 SVG、脚本或数据绑定。Lucide 0.468.0 的 36 个精选图标内置于 `shared/icon-catalog.ts`，许可证见 `licenses/lucide.txt`；严禁运行时访问图标 CDN 或外网字体。系统字体取本机可用字体，编辑器、预览与派生封面均支持新节点。
- 标题和图标保存依赖迁移 `0012_card_titles_and_icons.sql`，部署新代码前必须先执行迁移；迁移扩展节点白名单和尺寸下限，保留已有画布节点与配置。运行 `pnpm test:ornaments` 验证前后端校验、图标白名单、文字安全转义、缩放、主题和 SQLite 迁移/保存读取；测试使用临时编译目录和内存数据库，不修改项目数据（Node 22.13+）。
- 雷达扫描、数据流光、电路脉冲、能量核心、工业流线和网格扫描是纯视觉背景节点，只保存现有颜色、透明度等轻量配置，不接受外部资源或数据绑定。动画必须使用浏览器可合成的 CSS 变换/透明度并尊重 `prefers-reduced-motion`；编辑态、预览态与模板缩略图复用同一渲染，派生封面输出可识别的静态代表画面。保存依赖迁移 `0013_animated_decorations.sql`，当前封面渲染器版本为 6；`pnpm test:ornaments` 同时覆盖六种动画点缀的前后端约束、主题、缩放、封面和 SQLite 保存读取。
- 可组合看板节点依赖迁移 `0020_composable_dashboard_nodes.sql`。`scene-3d` 只接受可访问的 3D 项目 ID 与交互开关，`asset-detail` 只保存标题、空态、指标数量、元数据显示开关和主题颜色；两者都不得保存资源引用或数据绑定引用。修改对应契约后运行 `pnpm test:composable-dashboard`，并同时执行前端检查与构建。
- 同一页面中的自动轮播图也必须共享一个低频时钟调度器；编辑态不自动轮播，最后一个订阅者卸载后必须停止计时器。链接、按钮和表单控件在编辑态不得触发跳转或业务交互，只能在预览/运行态操作。
- 全屏切换组件保存进入/退出文字、外观和禁用状态，最低尺寸 120 × 48，依赖迁移 `0014_fullscreen_toggle.sql`。编辑态只允许选中和配置；预览/运行态通过浏览器 Fullscreen API 让当前画布视口进入或退出全屏，并监听原生 `fullscreenchange` 同步全部同类组件状态。全屏缩放采用等比例铺满（cover），取消普通画布的 48px 留边和 1 倍缩放上限；不同屏幕比例下居中裁切溢出部分，禁止拉伸或保留黑边。浏览器权限关闭、目标容器缺失或请求失败必须显示错误并记录节点上下文，禁止静默失败。
- 画布元数据与组件节点分开持久化；图片、视频、模型和数据流不得内嵌进节点 JSON，只保存受控资源 ID 与数据绑定 ID。
- 当前图片资源只接受 PNG、JPEG 和 WebP，单张不超过 8 MB；服务端必须校验扩展名与文件签名，图片组件最多引用 1 个资源，轮播图最多引用 12 个同项目资源。
- 工作台 `#/resources` 是统一资源入口，按用户有权访问的项目切换范围，列出系统内置模型和该项目上传到对象存储的模型、图片、视频、音频。上传资源继续遵守项目编辑权限；视频只接受 MP4/WebM 且不超过 100 MB，音频只接受 MP3/WAV/OGG/M4A/AAC 且不超过 30 MB，服务端必须校验扩展名与文件签名。
- 系统内置资源在前端标记为不可删除，服务端也必须通过固定白名单拒绝删除，不能只依赖禁用按钮。用户资源删除前由服务端重新统计当前项目画布和独立 3D 场景的资源引用；有引用但请求未携带显式确认时返回 `409 resource_in_use`，确认后才删除元数据和对象存储文件。删除不自动清空引用，失效引用必须保留用于定位；引用统计、删除结果和对象存储清理异常必须可观察。
- 资源预览必须继续经过项目访问控制：模型复用 `Model3DNode`（包括内嵌动画播放），图片、视频和音频使用受保护的资源内容接口。资源库媒体表依赖迁移 `0015_resource_library_media.sql`，实现或部署相关接口时运行 `pnpm test:resources`。
- 资源库中的“AI 场景底座”支持单图极速背景、环拍视频或多图写实漫游两条输入路径，在浏览器本地检查格式、数量、体积、场景名称、后续优化偏好、镜头范围和背景宽度。单图流程必须先将原图保存为项目图片资源，再通过同项目接口同步生成内嵌原始图片纹理、使用 `KHR_materials_unlit` 的自包含 GLB；生成模型作为普通项目模型资源发布，来源图片 ID、生成参数、像素尺寸和米制尺寸由迁移 `0017_scene_background_models.sql` 定义的字段持久化。背景宽度默认 10 米，高度按原图比例计算；产物只承担固定视角视觉背景，前端不得开放尚未实现的小范围移动或自由漫游选项，也不得宣称包含深度、遮挡、碰撞、物理信息、2.5D 深度重建或 Gaussian Splatting。生成模型在资源预览和首次设为主模型时默认正视、以 2.2 倍显示比例充满视口、关闭自动旋转与网格，之后仍可由用户在 3D 编辑器调整。环拍视频/多图路径继续只导入原始素材并明确 GPU 重建服务尚未接入。导入或生成成功必须刷新资源列表；多文件部分失败或模型生成失败必须保留可追踪的来源资源 ID 和真实错误，禁止静默关闭或假成功。输入边界由 `apps/web/src/pages/scene-background.ts` 统一维护，GLB 生成由 `apps/api/src/scene-background-glb.ts` 维护，修改后运行 `pnpm test:scene-background`。
- 2D 与 3D 项目封面必须是已保存项目的真实渲染截图，禁止用概念图、设备符号或简化 SVG 假装实景。项目列表中的可编辑项目按队列复用正式 CanvasSurface / Model3DNode，等待模型、嵌入场景与图片加载完成后生成 960 × 540 PNG；每次只挂载一个截图项目并在结束后释放资源。WebGL 必须通过注册的同帧 render + capture 获取真实画面，不能永久开启 preserveDrawingBuffer。保存文档会在同一事务将封面置 pending 并递增 coverRevision；被引用 3D 更新也必须使引用它的 2D 封面失效，改名无需重拍。PUT /api/v1/projects/{id}/cover 同时校验 sourceRevision 与 expectedCoverRevision，拒绝旧截图覆盖新内容；GET cover.png 需要项目读权限并使用私有 ETag 缓存。PNG 二进制单独存在 PostgreSQL project_covers（最大 2 MiB），不写 Base64 到项目文档、不占模型对象存储。V3 迁移删除历史概念 SVG 并等待首次有写权限的列表访问补拍；失败必须显示错误和重试入口，禁止替换成示意图或返回假成功。详见 docs/project-covers.md。
- 配置变更应生成版本；发布、回滚和当前生效版本必须可追溯。

## 模型与性能规则

- 内置公开样例使用 `shared/builtin-models.ts` 的固定 ID 白名单，包括 `builtin:aqua-helix-hd-v1` 及 `shared/workshop-models.ts` 中的 21 类当前车间模型（地坪 v3，其余 v2）、`shared/workshop-models-v1.ts` 保留的 13 类初版模型，以及 `shared/workshop-models-legacy.ts` 保留的 v2 地坪。模型保存在 `apps/web/public/models/`，GLB 文件名含内容哈希；画布仍只保存 ID，API 在项目授权后认可该白名单。未知内置 ID 必须报错，禁止将任意 URL 当作内置模型。此静态分发仅适用于项目自带的公开演示资产；用户上传继续使用原有项目归属、权限、大小检查和 R2 流程。样例源文件和生成说明见 `demo-assets/aqua-helix-hd/README.md` 与 `demo-assets/workshop-kit/README.md`。
- 车间模型由 `node scripts/generate-workshop-models.mjs` 确定性生成，米制、Y-up、GLB 自带动画；不可在同一版本 ID 下替换内容。`shared/workshop-layout.ts` 只生成正式独立 3D 实例与设置，当前为 98 个实例，不引入第二套保存 schema。示例车间属于可重复使用的 3D 场景模板，不在空白场景或旧模型场景中设置专用搭建/替换按钮。套用模板必须检查资源完整性、性能预算及包含删除项的补丁预算，保存后才生效。禁止后台自动覆盖场景、默认绑定真实业务资产或把 AGV 动画当成调度/物理仿真。生成时须对完整动画周期进行 60 Hz 网格 AABB 检查并验证动态实例扫掠包围盒互不重叠；排除支撑地坪，该离线检查不等于实时物理。新版 GLB 内嵌确定性 PBR 贴图。修改预设同时执行生成器、`pnpm test:workshop-motion`、`pnpm test:models`、`pnpm test:standalone-3d` 和前端 check/build。
- `model-3d.props.presentation` 由 `shared/model-presentation.ts` 统一校验，保存灯光模式、外壳模式、水流显隐和 0–1 拆解比例；旧数据缺失该字段时显式迁移为标准灯光/原始材质/水流显示/合拢。编辑器、2D 画布与运行预览复用 `Model3DNode`；检查面板使用 `ModelPresentationPanel`。不得将整套模型节点树或动画轨道写入配置。
- 检视能力由 GLB 节点 `extras` 标记：`label` 是显示名，`inspectionShell` 标记外壳，`category` 的 flow/animation 标记水流，`explodeOffset` 为有限三维本地位移。渲染器验证元数据并按实际能力启用控件；拆解时隐藏水流，合拢后恢复所选开关。原始节点名继续用于持久化绑定，中文显示名不得替代唯一节点标识。
- 切换模型资源时清除上一资源的节点外观与变换覆盖；材质修改必须按对象克隆，并在切换/卸载时释放克隆、纹理、几何和摄影棚后处理资源。摄影棚使用本地生成的环境反射；透明画布背景使用直接渲染以保留 alpha，不启用会覆盖透明度的辉光后处理。
- 内置模型变更运行 `pnpm test:models`；接口验收使用 `pnpm smoke:models`，只可对明确隔离、已迁移的本地测试 Worker 执行，具体命令见 README。
- 平台加载 GLB/GLTF 后默认循环播放文件内嵌动画，并允许在轻量画布配置中保存播放开关和速度；动画轨道仍属于模型资源本身，不复制进画布 JSON。编辑器与预览必须复用同一播放设置，整机自动旋转仍是独立场景配置，不得替代设备内部动画。
- 首期只接收 glTF 2.x 的 `.glb` 与 `.gltf`；单文件 `.gltf` 必须内嵌 buffer 与纹理，包含外部 URI 时必须明确拒绝并提示转为 `.glb`。保留原始文件、优化产物和可复现的导入检查报告。
- 画布节点只保存受控模型、图片、视频或音频资源 ID；Cloudflare 验证环境的文件通过 `PROJECT_FILES` 绑定进入 R2，客户现场通过对象存储适配层迁移到 MinIO/S3，不把文件二进制、Blob URL、对象存储公网 URL 或临时本地路径写入画布配置。
- 导入必须检查：文件体积、节点数、三角面数、材质数、纹理尺寸、缺失纹理、重复节点名、坐标单位和包围盒。
- 优化路径为几何压缩（Meshopt/Draco）、纹理压缩（KTX2/Basis）、删除无效资源、按车间/楼层/区域拆包；不要以降低渲染质量来隐藏资源问题。
- 资源应按需加载：首屏优先核心模型、状态和 KPI；非可见区域延迟或预加载。静态资源使用带内容哈希的缓存策略，实时数据和项目配置不可被错误缓存。
- 高频数据必须批量合并刷新，禁止每条消息直接触发一次 3D 渲染或 DOM 更新。
- 大量同类设备优先使用实例化、区域裁剪和 LOD。WebGL 或硬件能力不足时，展示可诊断的错误与设备信息，不伪装为正常运行。
- 同一 3D 场景中的多个模型实例必须共享一个 WebGL renderer、相机、灯光、控制器和动画循环；相同模型资源在单个场景内只加载和解析一次，实例克隆复用几何与原始材质。场景离开视口时暂停帧更新，资源结构变化或组件销毁时必须停止动画、释放派生材质/环境贴图/几何资源和 WebGL 上下文。不得退回“一个实例一个 canvas/renderer”的实现。
- 独立 3D 项目编辑实例时复用共享运行层中的 Three.js `TransformControls`：选中实例才挂载操作轴，移动使用世界坐标，缩放使用实例本地坐标，X/Y/Z 和等比缩放均可用。拖动期间必须禁用 OrbitControls，松开后把有限且符合 API 边界的变换写回 React 场景草稿；操作轴自身的点击不得清空选择。右侧数值输入继续作为精确编辑入口，拖动不会绕过显式“保存场景”流程。预览态和旧单模型入口不得显示实例操作轴。
- 地坪分区与标线使用 `scripts/workshop-floor.mjs` 的单层分区网格，禁止再用毫米级薄盒子叠铺地坪。普通轨道视角的 near/far 由 `camera-clipping.ts` 按场景包围球和中心沿镜头视轴的有符号深度动态拟合，缩放后也必须更新；Rapier 行走使用独立裁剪配置。地坪拓扑回归并入 `test:workshop-motion`，相机精度回归并入 `test:scene-runtime`。既有项目通过「更新内置模型」显式升级资源引用，保留实例变换、名称、绑定和场景设置，校验资源/补丁预算后由用户保存；不得自动替换旧 ID 对应文件。
- 共享运行层摄影棚模式使用单张 2048 × 2048 PCF 主光投影，阴影相机按场景范围拟合；标准模式关闭投影，销毁时释放阴影贴图。不得按设备增加阴影光源。独立场景切换编辑/预览时清除选择，禁止默认选中地坪；运行态设备选择只显示贴地脉冲环，不显示编辑包围框或操作轴。
- 独立 3D 项目的数量上限不能替代复杂度预算；服务端保存前必须根据模型检查元数据计算唯一资源字节、网格实例和动画实例。当前实现尚不等于城市级流式场景：区域分包、视锥/距离加载、LOD、同构静态网格 GPU Instancing 和离屏降频仍需在真实客户模型压测后逐项接入，宣传不得提前声称已完成。
- 首期性能目标待首个真实项目压测后固化；在此之前不得把模型体积、首屏时间或帧率写成无依据的承诺。

- 独立 3D 的公共流体组件使用 `shared/fluids.ts` 的同级 `scene.fluids` 契约：气体／液体／熔融体都支持连续与扩散形态、颜色、鼠标路径、方向及播放设置。未完成路径只存在编辑会话；应用后通过场景 revision 事务保存，预览、嵌入场景和封面共享运行层。最多 32 条、每条 64 点、256 粒子；禁止每条流体建立 renderer/时钟或逐帧重建几何，禁止项目固定路径分支。Java 在既有 settings JSONB 内部存储并在 API 拆出同级字段，settings 更新必须保留流体；历史 Worker 依赖 `0023_scene_fluids.sql`。修改后执行 `pnpm test:fluids`、`pnpm test:fluids-browser`、`pnpm test:fluids-scene-browser`、`pnpm backend:smoke:fluids`、scene-runtime 回归、前端 check/build 与 Java 契约/后端验证。说明和操作见 [可配置流体组件](./docs/configurable-fluids.md)；当前为视觉流动，不宣称物理流场或 GLB 导出。

## 开发与质量要求

- 当前技术栈：pnpm workspace；`apps/web` 为 React + Vite + TypeScript；`apps/api` 为既有 TypeScript Cloudflare Worker + D1；`apps/backend` 为 Java 21 + Spring Boot 独立部署后端，`deploy/local` 为 Docker Compose。启动和验证命令以根目录 `README.md` 与各应用 `package.json` 为准。
- 2026-09-17 后端设计补充：用户要求同时支持客户独立部署和平台统一托管，并说明有充足 Java 后端团队资源；设计首选据此调整为 Java + Spring Boot、PostgreSQL、Valkey、S3 兼容存储与 Docker Compose，详见 [后端架构设计建议](./docs/backend-architecture-proposal.md)。历史 Node/Fastify 选型属于旧建议。Java 本地首版现已实现，运行方式、接口与未完成范围以 [Java 后端说明](./apps/backend/README.md) 为准。
- 2026-09-18 已把 `factory-digital-twin-config` 的云端业务快照和旧 Wrangler 本地状态中的 3D 工厂、组合大屏、上传模型与图片导入本地 PostgreSQL/SeaweedFS，结果与可复现命令见 [Cloudflare 到本地迁移记录](./docs/cloudflare-to-local-migration.md)。Cloudflare 身份、密码散列和会话不得迁入 Java；项目成员要显式映射到本地账号并留下审计记录。云端与旧 Wrangler 源数据保持不变，后续不得把旧 D1 当作本地开发的写入目标。
- Java 契约生成物由 `pnpm backend:contracts` 从现有共享 TS 定义生成；变更相关定义后执行 `backend:contracts:check`、`backend:verify` 和本地 `backend:smoke`。所有 `.env`、随机凭据、备份均不得提交。
- `@Transactional` 服务经 Spring CGLIB 代理调用，调用方不得直接读取代理实例字段；依赖使用构造注入或服务方法。首次配置读取和模拟命令回归须覆盖真实代理，不能只测试手工创建的未代理服务。
- 2D/3D 动作的本地真 API 回归使用 `pnpm backend:smoke:twin-actions`，仅访问本机 18080、不占用 8790；默认清理自己创建的临时项目和账号。`--keep-fixture` 成功后保留用于浏览器验证，之后必须以 `--cleanup` 按本地记录精确清理，禁止删除记录后跳过清理或触碰业务项目；详见 [联动交互验证](./docs/linked-2d-3d-interactions.md#本地-java-api-冒烟测试)。
- Java 保存旧 `model-3d` 节点时，仅对缺失字段应用由原 TS 校验器生成的兼容默认值，包括缺少 `modelInstances` 的旧单模型画布；显式 `null`、类型错误和未知字段仍必须拒绝。画布 `props` 按节点 `type` 精确校验，禁止同时校验所有组件的属性联合而返回无关错误；错误去重并保留请求 ID 日志。`backend:smoke` 覆盖旧画布保存、读取和与前端兼容结果的一致性。
- Java 迁移时保留现有 HTTP 契约、错误与权限语义，并按版本定义新增 WS 契约；将当前 TypeScript 共享校验逐步提取为语言无关 schema 与共享正反样例，并验证 Java/TypeScript 结果一致。生成类型不能替代服务端业务校验，不能同时维护会漂移的两套配置定义。
- `apps/api/wrangler.jsonc` 绑定 D1 `factory-digital-twin-config`。Worker/D1 迁移只存放在 `apps/api/migrations/`；Java/PostgreSQL 迁移只存放在 `apps/backend/src/main/resources/db/migration/`，由 Flyway 管理，并且必须在迁移命令成功返回后才能让对应 API 依赖新表。
- 前端 API 地址只能从 `VITE_API_BASE_URL` 获取；本地默认值仅用于开发。生产域名、真实客户接口与密钥不可写入源码或提交 Git。
- P1 本地 REST 竖切的模拟设备由根目录 `pnpm dev:mock` 启动，只监听 `127.0.0.1:8790`。完整配置、状态切换和失联恢复步骤以 [本地 REST 设备联动测试](./docs/local-rest-device-runtime-test.md) 为准。
- 流体行业案例当前采用第二版：`scripts/generate-fluid-cases-v2.mjs` 生成标准 GLB，`scripts/update-fluid-cases-v2.mjs` 通过 Java 正式 API 原地更新初版日志明确记录的三个案例，保留六个项目 ID；规模、入口、工艺边界与验收见 [第二版案例说明](./demo-assets/fluid-cases-v2/README.md)。更新前保存原配置与资产映射快照，使用独立日志、资源哈希和 revision 检查，外部变更或 API 错误必须中止。初版生成/导入脚本仅保留历史复现用途。配套模拟源由 `deploy/local/fluid-demo.yml` 的 `source` 服务启动，与 `dev:mock` 互斥占用本机 8790；修改源代码后重启该服务。设备及看板必须标注模拟，源停止时保留真实失联状态。案例是预设流体视觉动画与业务绑定；指标中的简化热量/水量关系不等于 CFD，也不代表数据驱动液面或真实生产接入。
- “三相流体实验舱”是独立的气体/液体/熔融视觉展示项目，使用标准 GLB 动画与内嵌贴图，不修改已有行业案例或共享运行时；生成、导入、修订保护与验证见 [实验舱说明](./demo-assets/matter-lab/README.md)。修改资源后运行 `node scripts/build-matter-lab.mjs` 与 `node scripts/test-matter-lab.mjs`，导入仅能针对日志拥有的项目并校验 revision，不能冒充 CFD 或真实采集数据。
- 新增接口、配置 schema、导入文件格式或发布流程时，必须同步给出可验证的样例、校验规则和错误信息。
- 对数据接入、模型导入、发布流程等关键链路保留请求/任务 ID、项目 ID、版本 ID、耗时和失败原因，便于交付现场定位。
- 配置与运行态必须使用同一份被版本化的项目 schema；禁止分别维护会漂移的重复配置。
- 对外宣传页必须明确区分“交付场景示例”和有验收依据的真实客户案例；没有客户授权、项目证据与可核验结果时，不得使用客户名称、Logo、效果数字或暗示已经落地的文案。行业入口可以用于筛选场景与模板，但不能伪装成实际交付记录。
- 案例化模板允许使用虚构企业、城市、现场名称和符合行业语义的模拟指标增强项目感，但宣传页、模板中心和画布初始数据必须同时显示“虚构演示”或“模拟数据”；改为真实客户案例前，必须取得客户授权并补充可核验的交付证据。
- 依赖或服务端失败应暴露具体原因和上下文，不要添加静默兜底、宽泛 catch 或虚假的成功提示。
- 新功能至少覆盖：正常路径、输入校验失败、外部连接失败、数据陈旧/断线、权限失败和性能边界之一（按功能相关性选择）。
- 当前项目由用户单人开发，默认直接在 `main` 修改；无论改动规模或风险高低，Codex 都不得自行创建、切换或保留临时分支。只有用户明确提出创建分支时，才允许使用其他分支。
- 修改前仍必须检查工作区状态并保护用户未提交改动，禁止把无关文件混入修改或提交。

### 平台界面外观

- 工作台的项目、模板、资源和用户列表页按视口宽度伸展，只保留随屏幕变化的页边距；顶部导航与内容对齐，卡片网格随可用宽度换列，窄屏不得横向溢出。不要给整个工作台列表页设置固定最大宽度。
- 平台浅色/深色模式由 `apps/web/src/theme/` 统一管理，根元素 `data-ui-theme` 与 `--ui-*` 语义颜色是唯一外观入口。登录、宣传、项目/模板/资源、弹窗、编辑器及运行态控件必须使用同一套主题状态，不得在各页面单独维护开关或使用 CSS 反色滤镜。
- 无手动偏好时跟随系统；手动选择保存在浏览器本地并支持恢复跟随系统及跨页签同步。读取/保存偏好失败必须带上下文记录并提示，不能阻塞登录或把保存失败当成成功。
- 平台界面主题与交付画布主题是不同配置：切换 UI 不调用项目保存接口，不修改画布 JSON、模板缩略图、模型材质/灯光、图片或视频。编辑器工具栏、图层、属性和系统浮层适配 UI 主题；作品本身继续忠实显示已保存配色。画布主题通过现有“主题”功能显式修改和保存。
- 新增界面颜色需同时提供浅/深语义值，覆盖普通/悬停/选中/禁用/错误状态，文字保证可读对比度。变更主题后执行 `pnpm test:ui-theme` 及前端 check/build，并检查登录、列表、弹窗、2D/3D 编辑器与公开宣传页。
- 属性栏的分组标题、只读值和禁用控件使用 `--ui-readonly-bg`，与有边框的可编辑字段区分；操作必须使用有边框、背景及 hover/active/focus 状态的原生按钮，移除类操作使用危险色。禁用样式须覆盖复合控件外壳和禁用 fieldset 的子控件，不得只降低整组透明度；未启用自定义颜色时显示只读“原始材质”，不展示虚构的材质色块。
- 所有单选下拉统一使用 `apps/web/src/components/Select`，禁止新增原生 `<select>`。下拉触发器和列表弹层共同适配 UI 主题、键盘导航、焦点恢复、必填校验、长文本与弹窗/全屏；禁用字段及禁用 fieldset 内的控件使用灰底。运行 `pnpm test:select-browser` 做真实浏览器回归；开发/CI 通过 `PLAYWRIGHT_MODULE_PATH` 提供已安装的 Playwright，不在运行测试时下载依赖或浏览器。

### 身份与权限基础

- 登录页的品牌展示使用 `apps/web/src/auth/LoginShowcase.tsx` 和公开内置模型，复用共享 3D 运行层；不读取客户项目或资源。展示区禁用原生拖拽、选中与右键保存菜单（不等于资源防下载），保留暂停/播放控制并响应系统减少动态效果设置。模型加载错误必须可见，展示失败不得阻塞账号密码表单。
- 所有 `/api/v1/projects` 及后续配置和数据源接口默认需要已验证的服务端会话。Java 后端的项目公开发布是明确例外：项目编辑者或管理员通过登录接口创建/取消发布；持有签名分享令牌的访客只能读取 `/api/v1/publications` 下发布范围内的当前保存项目，以及只读点位 WebSocket。每次读取和推送都校验发布状态与项目范围；取消发布或重新发布立即废止旧令牌。链接范围最多 32 个关联项目，发布者必须对每个项目有编辑权限。公开资源接口只提供当前文档引用的资源；已签出的对象存储下载 URL 最多仍可用 5 分钟。此功能只在 Java 后端实现，Worker 验证环境不支持。除健康检查、首次初始化、登录和上述受控公开读取外，禁止匿名访问。
- 首个管理员只能在 `users` 表为空时，通过服务端 `BOOTSTRAP_TOKEN` 初始化；其唯一登录名固定为 `admin`，密码仍由初始化人员设置。不得提供公开注册、默认密码或把初始化令牌下发进构建产物。
- 登录接口以“账号或邮箱”为正式契约，并暂时兼容旧客户端的 `email` 请求字段；登录名统一转为小写且只允许字母开头的字母、数字、点、下划线和连字符。迁移 `0016_admin_login_name.sql` 为既有部署中最早创建的平台管理员分配 `admin`，新部署由初始化流程显式写入；部署后运行 `pnpm test:auth`，不得在查询中硬编码账号特判。
- 既有 Worker 本地与 Cloudflare 验证环境仍是可清理的测试环境，密码长度临时按 6 至 256 个字符校验，前后端统一读取 `shared/auth-constraints.ts`；转入真实生产数据前必须恢复至少 12 位的密码强度要求并轮换全部测试凭据。不得为单个账号增加隐藏特判。Java 首版新账号要求 12–256 字符，不迁移或降低该安全边界以匹配旧测试密码。
- Cloudflare Worker 的 Web Crypto 对 PBKDF2 最多支持 100,000 次迭代；当前新密码记录固定使用该上限，并在每条用户记录中保存实际迭代次数。切换运行时或密码算法时必须设计显式升级流程，不得直接提高默认值导致线上登录或初始化失败。
- 当前全局角色为 `platform_admin`、`delivery_manager`、`viewer`；项目成员角色为 `owner`、`editor`、`viewer`。权限判断只能在服务端执行，前端隐藏按钮不构成授权。
- 账号另有独立的 `2d`/`3d` 模块授权。平台管理员固定拥有两个模块；其他账号可授予任意组合或均不授予。模块授权是进入对应项目类型的前置条件，项目成员关系与全局角色继续决定项目范围和操作；每次请求从数据库读取授权，使调整对已有会话立即生效。管理员在 `#/users` 使用 `GET/POST /api/v1/users`、`GET/PUT/DELETE /api/v1/users/{id}`、`POST /api/v1/users/{id}/restore` 和 `PATCH /api/v1/users/{id}/modules` 管理账号；`PUT` 同时编辑资料、全局角色与模块权限，可选重设密码并撤销原会话。`DELETE` 停用账号并立即撤销会话，保留审计与项目关联记录以便恢复；已停用账号必须先恢复才能编辑；管理员不能删除自己或移除自己的管理员角色，最后一个启用的管理员不能被移除。新账号必须明确提交 `modules`，既有账号由 Java V5 / Worker D1 0024 迁移保留两个模块。跨项目的 2D/3D 场景引用和声明式动作也必须检查目标模块权限。相关回归运行 `pnpm test:module-access`、`pnpm test:user-management-browser`、`pnpm test:twin-actions-api`、`pnpm backend:verify`。
- 项目名称修改要求平台管理员或项目编辑权限；项目永久删除只允许平台管理员或项目负责人。删除必须清理项目关联配置，外部对象存储清理失败时必须记录上下文并向调用方返回明确警告，禁止伪装成完全成功。
- 用户、角色、会话和项目成员关系使用应用自有 schema 与 API 契约；Cloudflare Worker/D1 只是当前适配器。后续迁移到 Java/Spring Boot/PostgreSQL 时必须保留这些接口与权限语义。
- 前端和 API 的认证交互默认同源，以 HttpOnly、`SameSite=Lax` Cookie 维持会话；本地开发通过 Vite `/api` 代理。跨站部署必须先完成 Cookie、CSRF 与反向代理设计，不能临时放开任意 CORS。

## 安全与交付

- 密钥、私钥、证书、真实客户模型、真实接口样本和生产数据不得提交。使用本地 `.env`、受控密钥存储和脱敏样本；新增环境变量必须写入 `.env.example` 与部署文档。
- 客户内网部署优先使用服务端数据网关/采集器与 Docker 化交付。网络拓扑、白名单、证书、数据访问权限和日志留存属于上线前验收项。
- 发布前检查至少包括：模型资源、资产映射、数据源连通性、字段/阈值、权限、性能结果和版本信息。任一阻断项失败不得发布。
- 当前临时云入口为 Cloudflare Worker；它只用于早期 API 验证，不能成为前端、数据模型或部署流程的隐性绑定。资源现状和迁移边界见 [Cloudflare-临时部署与迁移约定.md](./Cloudflare-临时部署与迁移约定.md)。
- Cloudflare 验证环境通过同一个 Worker 域名提供 Vite 静态前端与 `/api`，保持 HttpOnly Cookie 同源。账号未启用 R2 订阅时可以省略 `PROJECT_FILES` 绑定以发布其余能力，但健康检查必须标为 `degraded`，用户上传模型与图片的上传/读取必须分别明确返回 `503 model_storage_not_configured` 或 `503 image_storage_not_configured`；禁止改存 D1、静默丢弃文件或伪装上传成功。固定白名单内的公开样例随前端分发，不依赖 R2。

## 当前开发顺序

2026-09-21 用户将当前优先级调整为可配置点位反馈驱动，先用长沙凯德案例验收。已实施的模拟 WS / 点位绑定 / 关节依赖 / 反馈条件工序 / 简化碰撞事件及限制见 [点位驱动契约](./docs/data-driven-twin.md)。真实点位链路不得使用演示时钟推进姿态；下面旧演示计划仅适用于显式离线演示，不代表已完成物料交接或生产采集。

- 点位配置以 `shared/twin-drive.ts` 为单一契约，存独立 `twin_drive_documents` revision，不塞入 2D 画布或 localStorage。Java 独立 WS 使用 Cookie/Origin/项目权限；命令带 revision + commandId，ACK 不等于运动到位。
- 点位/topic、后端自动源和接入测试仅在 3D 编辑页配置；预览只订阅反馈，不放配置表或手动控制，不发送 reset/run/move。`simulation.enabled` 保存后 Java 后台独立运行，关闭全部浏览器不能停止源；按完整项目 topic 集合显式订阅并核验回包映射。topic 是本平台 WS 主题，不能宣称已经接入 MQTT。自动模式拒绝手动命令；循环按反馈到位推进，不复位瞬移。契约和验收见点位驱动文档。
- 点位配置使用独立工作区，按选择数据源、选择数据、绑定部件、接入测试四步组织；姿态与模拟工序使用表单，JSON 仅作高级入口。当前可驱动模型的来源仍只有平台模拟 WebSocket；API 轮询的设备数据展示不等于运动接入，外部 WebSocket 订阅尚未实现，两者必须明确暂不可选。不得为界面选择增加没有实际执行链路的持久化字段或伪造连通状态。
- 数据工作区只使用已保存场景，进入前须保存场景草稿；编辑期间原场景保持挂载且不可交互，返回不得新建 renderer。保存与返回操作在小屏常驻，草稿跨步骤保留，未应用 JSON 必须阻止保存，离开未保存内容须明确确认。运行 `pnpm test:twin-workspace` 验证小屏、浅深主题、只读、草稿及重绑；使用既有 Playwright/Chromium，可通过 `PLAYWRIGHT_MODULE_PATH`、`CHROMIUM_EXECUTABLE` 指定路径，不临时下载依赖。
- 跨模型复用通过显式重绑实现：先加入并保存新模型，再将旧模型全部动作/碰撞目标映射到新模型、应用草稿并保存点位配置，最后才可移除旧模型并保存场景。保留点位/topic、动作参数、父关节 ID、工序和碰撞规则；唯一同名只作建议，漏映射、重名、旧资源、重复驱动和不合法依赖必须阻断应用，不静默丢弃。关闭驱动不解除服务端场景引用保护；新旧模型共存继续遵守资源预算。更换后必须重新核对坐标、轴心、零位、行程和碰撞盒，不宣称自动校准。运行 `pnpm test:twin-drive` 包含重绑回归。
- 姿态只由实际反馈值驱动；缺失、过期、断线时冻结并提示，禁止时钟补走、隐式重发或把模拟值标记为真实设备数据。绑定与原生动画冲突必须在保存和运行时暴露。
- 碰撞是配置的浏览器 OBB enter/exit，不等于连续检测/工业安全互锁；不得自行修改真实反馈以“避障”。每次改动须执行 `pnpm test:twin-drive`、`node scripts/test-kaide-twin-drive.mjs` 与 `node scripts/test-kaide-twin-runtime.mjs`（真实模型可用时）、运行层回归与前后端 check/verify。

2026-09-15 根据用户确认的交付目标调整如下。既有身份、权限、资产与数据链路继续保留；演示配置无需等待客户实时接口接通。生产采集、数据库迁移和模型处理服务仍分别规划，本次方向确认不代表这些服务已实施。

1. 先定义并验证资源准备、实例内部稳定节点、部件角色、锚点、关节和动作配置，打通一个静态夹具抓取工件、搬运、释放的可保存流程；已有 GLB 动画作为另一种动作来源接入。
2. 增加工序编排、统一演示时钟、路径、顺序/并行、等待、路线分支、物料附着/交接和循环复位，覆盖四托盘及二层升降流程。
3. 将材质、灯光、地面、粒子、灯带、镜头跟随与状态卡配置接入同一套工序状态，编辑预览与交付运行复用运行层。
4. 完成可复用模板、发布版本与纯前端离线导出；用凯德全流程和替换模型/业务参数后的流程验证，不能以重新写客户专用脚本完成验收。
5. 每步同时落实模型质量、动作接触、配置错误和目标设备性能验证；按证据推进模型优化与资源预算调整，后续真实数据驱动保持与模拟演示显式区分。

### 工艺演示配置约束（待实现）

- 模型资源、实例节点、业务资产是不同身份。动作、镜头和特效通过实例与稳定节点/锚点绑定；业务设备继续关联 `assetId`。客户节点名、动作时间和路径只能进入项目配置或资源，不能硬编码进平台运行逻辑。
- 动作参数和工序编排保存为有版本、可校验的正式配置；已有动画曲线仍由模型资源持有。编辑器、预览和离线发布共享配置语义，不在 localStorage 或 2D 节点 JSON 另存独立场景的完整副本。
- 动作、镜头、特效和流程卡片共享工序状态与演示时钟；抓取/释放、机构交接、逐帧定位和循环必须能一致还原物料归属与姿态。随机取料要支持配置范围、占用校验和可复现种子。
- 运动与业务规则通过内置动作和受控参数配置，不开放任意用户 JavaScript。静态模型必须具备可独立运动的部件；缺少部件、转轴、接触面或存在几何重叠时明确记录并进入模型准备/修复流程，不宣称自动识别任意机构或保证绝对无碰撞。
- 纯演示交付支持 HTML/CSS/JS 与本地资源组成的离线目录，目标是经验证的现代桌面浏览器；制作环境可以使用 Blender 等工具，客户播放不依赖 Python/Node.js。实时数据版仍遵守数据网关与授权要求，离线包不得携带凭据或伪装为实时数据。

### 已有播放控制面板

- 3D 组件 `showControlPanel` 为布尔值，旧配置缺省 `false`，前后端均须严格校验。2D 属性栏和 3D 编辑器均可配置并随画布保存。运行预览开启后复用 `ModelPresentationPanel`，提供外壳、动画播放/速度、水流、拆解和灯光控制，按模型能力禁用不支持项；预览操作只保存在当前组件的临时查看状态，禁止写回画布或设备。更换模型、保存预设变化、关闭面板或重新打开时恢复保存配置；支持收起和恢复预设。2D 编辑画布不显示交互面板。

## 前端场景运行层（2026-09-10）

- 厂房场景评估与技术选择见 [评估记录](./docs/厂房场景架构评估与技术选型.md)，实施边界与验证见 [前端场景运行架构](./docs/frontend-scene-runtime.md)。本轮先落实前端，后端迁移、采集与模型优化服务暂缓。
- 多实例视窗由 `apps/web/src/scene/scene-runtime.ts` 拥有 renderer、相机和循环；React 只提交配置和接收事件。资源图变化通过 InstanceManager 增量协调，不能销毁仍在使用的 renderer 或源资源。替代此前“资源结构变化时整体释放”的规则；完整释放仅用于卸载或运行层销毁。
- 共享动画时钟只使用 renderer 回调时间戳计算帧差；首次启动、离屏或标签隐藏后的恢复以首个回调建立基线，首帧增量为零，禁止用 `performance.now()` 初始化后再减 RAF 时间戳。非法或倒退时间必须明确报错，模型、流体和镜头复用同一帧增量；回归并入 `pnpm test:scene-runtime`。
- ResourceManager 默认最多 3 个加载任务；同资源共享源对象、每实例持有租约，最后引用释放才销毁几何/纹理。取消必须区分失败，真实失败保留上下文并显示错误，不能返回假成功。
- Meshopt/KTX2/Draco 解码器本地分发；静态几何用 three-mesh-bvh，骨骼/morph 几何不得使用静态 BVH。拾取保留建筑遮挡。普通配置、实例增删及 resize 不复位相机。
- 修改运行层后执行 `pnpm test:scene-runtime` 和前端 check/build；浏览器验证脚本、诊断字段与范围见架构文档。LOD、区域卸载、GPU Instancing 和独立场景实时浮层仍是后续事项，不得宣称已完成。
- 2D 画布通过 `scene-3d` 节点引用独立 3D 项目，通过 `asset-detail` 节点显示当前业务设备；编辑态只允许移动、缩放和配置整个引用节点，预览态模型点击以 `assetId` 驱动同一画布内全部设备数据节点。引用节点不得复制源场景实例，设备详情不得直接依赖模型资源 ID。
- Rapier 0.20.0 通过 `@dimforge/rapier3d-compat` 延迟加载；每个行走会话独立拥有世界和胶囊角色，固定 1/60 秒、单帧最多 6 步。使用显式米制/Y-up/出生点/地面/边界/简化长方体契约，见 [Rapier 前端行走](./docs/frontend-rapier-navigation.md)。禁止直接将展示模型全部变为碰撞网格、默认生成无限地面或把未校验场景标为可行走。
- 行走期间相机只能由 WalkControls 驱动；切回、取消、配置变化、失焦、离屏与销毁必须管理输入和物理资源。当前静态行走要求关闭动画、自动旋转和拆解，并显示外壳；模型/显示配置变化退出行走。`walkScene` 仅是前端接入契约，碰撞体编辑与后端保存尚未实施，不能在画布 JSON 或 localStorage 私自增加持久化字段。
