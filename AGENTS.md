# 工厂数字孪生交付平台：项目协作规则

本文件只约束当前仓库。通用工程规则以全局 `AGENTS.md` 为准；两者冲突时，优先遵守更严格的规则。

## 产品定位

- 产品是面向交付人员的工厂数字孪生项目生成器，不是通用低代码/SaaS 大屏产品。
- 目标是以行业模板、标准 glTF 2.x 模型、资产台账和数据契约，快速交付 **2D + 3D 组合大屏**。
- 首期运行态是只读大屏；配置能力只向交付人员开放。
- 公开产品宣传页长期保留两套并存版本：`#/` 是原始绿色产品介绍版，`#/industrial` 是深色钢铁与警示橙视觉的工业风版本。两套页面都不依赖 API 或登录状态，但页面内不提供版本切换入口；不得用新版本覆盖或删除另一套版本。“进入平台”统一跳转到受身份验证保护的 `#/projects`。宣传页不得发起项目、用户或客户数据请求，也不得把尚未实现的能力、性能数字或数据连接状态写成既成事实。
- 当前详细方案的单一事实源为 [工厂数字孪生平台-启动准备与首期方案.md](./工厂数字孪生平台-启动准备与首期方案.md)。产品范围发生变化时，必须同步更新该文档和本文件。

## 首期范围与非目标

首期要支持：

- GLB/GLTF 模型导入、校验、浏览、对象选中/高亮、区域分组和镜头定位。
- 项目、3D 场景、资产与数据、2D 看板、预览与发布这五个配置工作区。
- 预制工厂模板：总览、产线、设备详情、能耗、告警。
- REST 轮询与 WebSocket 数据接入，以及 2D/3D 基于同一资产的联动。

首期明确不做：

- 无限画布、组件市场、用户脚本、在线协同编辑或“复刻 EasyV”。
- 浏览器直连 PLC、OPC UA、MQTT 或客户内网工业设备。
- 原始 BIM/CAD 直接渲染、GIS 城市级场景、倾斜摄影、云渲染和 Unreal。
- 为个别客户的模型或接口写进不可复用的特例逻辑。

## 不可破坏的领域约束

### 资产是关联中心

- `assetId` 是模型、资产台账、数据、告警、2D 组件和 3D 状态的唯一稳定关联键。
- `assets.id` 只作为数据库记录主键；`assets.asset_key` 通过 API 暴露为 `assetId`，是客户现场与跨模块共享的稳定业务编号，两者不得混用。
- `modelNode` 仅用于 glTF 模型节点映射，不能替代 `assetId`；模型节点名必须稳定且唯一。
- 当前首期在单个项目内以唯一 `modelNode` 建立资产映射；解除模型节点绑定只清空 `modelNode`，必须保留资产台账记录及稳定 `assetId`。
- 编辑器节点树直接来自已加载的 glTF 场景；子节点索引路径只用于当前编辑会话中的定位与选择，持久化的资产映射不得依赖可能随重新导出变化的索引路径。
- 模型视窗的对象点击与节点树选择必须双向同步；编辑态包围框高亮只属于当前会话，禁止写入画布 JSON，也不得通过修改模型材质实现。
- 模型节点位置、旋转和缩放只保存相对原始模型的稀疏覆盖，并以经过唯一性检查的模型节点名为键；未命名或重名节点不得保存覆盖。单个 3D 组件最多保存 100 个节点变换，禁止把完整场景树复制进画布 JSON。
- 模型节点颜色、透明度和显隐同样只保存以唯一节点名为键的稀疏覆盖，单个 3D 组件最多保存 100 个节点外观。运行时必须按被覆盖对象克隆材质，禁止直接修改共享原始材质而污染其他对象；覆盖重算或组件销毁时必须恢复原材质并释放克隆。
- 3D 场景的纯色/透明背景、环境光、主光源、初始视角、FOV 和整机显示比例属于轻量画布配置；整机显示比例必须在编辑器与预览中一致还原，视口滚轮缩放仅作为未保存的临时查看。HDR 环境贴图、天空盒与后续贴图文件必须走资源 ID，禁止把二进制或 data URL 写入节点 JSON。
- 3D 组件是一个场景容器，可显式保存最多 32 个 `modelInstances`；每个实例只保存稳定实例 ID、模型资源 ID、名称、显隐与整体位置/旋转/缩放。`resourceRefs` 必须按实例首次出现顺序保存不重复的资源 ID，并与实例引用集合完全一致；首个实例是主模型，当前节点树、节点级覆盖和资产映射只作用于主模型。缺少 `modelInstances` 的旧单模型节点按首个 `resourceRefs` 兼容读取，禁止迁移时复制模型文件或完整场景树。
- 资产映射中的重复 `assetId`、未匹配节点、重复匹配和缺失关键字段是发布阻断错误，不能静默跳过或默认绑定。
- 资产指标映射按行保存在 `asset_data_bindings`，通过资产记录主键关联稳定 `assetId`，不得塞入画布节点 JSON。每条映射必须引用同项目数据源，并显式保存唯一 `metricKey`、以 `$` 开头的源字段路径、值类型、单位和数据陈旧时间。
- 客户原始 `dataKey` 只能存在于数据源映射层，前端组件不可直接依赖客户原始字段路径。

### 数据网关是安全边界

- 浏览器只能访问项目配置和经过标准化、授权后的数据；客户 API 密钥、Cookie、令牌、内网地址不得下发到浏览器或提交到 Git。
- 统一数据事件至少包含 `assetId`、`timestamp` 与 `values`。字段定义必须有类型、单位、枚举/阈值和数据陈旧时间。
- REST 必须配置超时、频率限制、失败上下文和陈旧数据标记；WebSocket 必须有连接状态、心跳、订阅恢复和有界退避重连。
- 项目级数据源保存经过校验的 REST 轮询或 WebSocket 连接元数据；REST 可保存源数据时间戳的 JSON 路径，用于识别 HTTP 正常但数据停止更新。URL 禁止内嵌用户名、密码或常见敏感查询参数，认证只能保存服务端 `credentialRef`。当前仅有用于 P1 本地竖切验证的按资产 REST 即时采集端点；它必须由 `RUNTIME_POLLING_ENABLED=true` 显式开启，并且只允许访问 `RUNTIME_ALLOWED_HOSTS` 白名单中的主机。WebSocket 执行、凭据解析、生产级调度、限流和集中状态缓存尚未实现，界面不得把这些能力宣称为可用。
- 本地 REST 竖切对每次请求执行超时、拒绝重定向、256 KiB 响应体上限和严格 JSON 字段/类型校验；配置源时间戳路径后，以同一数据源映射中最短的 `staleAfterSeconds` 判定陈旧，超过 5 分钟的未来时间戳视为上游时钟错误。已保存 REST 数据源的连接测试仅允许项目编辑者执行，必须复用相同的运行开关、主机白名单、超时、重定向与响应体限制；字段发现只返回最多 200 个标量路径，递归深度最多 8 层、每个数组最多检查前 10 项，禁止把整份上游 JSON 持久化或写入画布。采集和测试的成功与失败必须记录 `requestId`、项目、耗时及错误码；按资产采集还必须记录资产上下文。运行态仅保留最后一次成功快照用于诊断，请求失败必须立即标记“失联”，时间戳过期必须标记“数据陈旧”，并明确说明旧值不是当前实时值。
- 真实数据错误、字段映射失败和设备失联必须被明确展示和记录；不得用假数据、空数据或“正常状态”掩盖。
- Mock/Excel 数据只用于演示和联调，运行界面必须明确标为模拟数据。

### 2D 与 3D 的配置关系

- 2D 与 3D 是同一项目配置台的两个工作区，不是两个割裂的产品或数据库。
- 2D 画布中的 3D 组件只展示配置摘要和独立编辑入口；模型导入、节点树、资产绑定、节点变换/外观、灯光与镜头统一在项目级独立 3D 编辑页完成。进入独立页前必须先保存画布，独立页仍加载并增量保存同一个 `canvasNodeId`，不得复制第二份 3D 配置；“保存并返回”后画布通过服务端版本重新加载结果。
- 运行态是一个组合页面：3D 主视区 + 2D 指标区/图表/告警/详情抽屉。所有跨区联动必须通过 `assetId` 和项目级事件定义实现。
- 首期 2D 使用有边界的 1920 × 1080 交付画布，只允许白名单组件拖拽、吸附、四角缩放、属性与静态数据配置和预览；当前白名单为 3D 模型、折线图、柱状图、面积图、饼图、环形图、雷达图、指标卡、环形进度、进度排行、状态矩阵、数据排名、实时告警、业务表格、事件时间线、矩形、圆形、大屏标题、背景点缀、雷达扫描、数据流光、电路脉冲、能量核心、工业流线、网格扫描、时间日期、标题、小卡片背景、小图标背景、科技面板、纯文本、文字超链接、图片、轮播图、按钮、全屏切换、Switch、多选框、单选框和下拉菜单，不扩展为无限画布、组件市场或用户脚本平台。科技面板是纯视觉背景节点，允许其他节点在其上方排版，但当前不承担分组、嵌套、联动或批量移动语义。
- 项目列表中，点击卡片封面进入画布编辑；卡片操作区的眼睛按钮必须直接进入该项目预览路由，不得先经过编辑页。
- 组件最小尺寸按类型约束：3D 模型为 360 × 240；折线图、柱状图和面积图为 240 × 160，饼图与环形图为 240 × 200，雷达图为 260 × 220；矩形为 240 × 160、圆形为 240 × 240；指标卡为 200 × 120、环形进度为 240 × 220、进度排行为 280 × 220、状态矩阵为 300 × 200、数据排名为 300 × 220、实时告警为 320 × 220、业务表格为 360 × 220、事件时间线为 320 × 240；大屏标题为 360 × 72、背景点缀为 200 × 72、雷达扫描和能量核心为 160 × 160、数据流光为 200 × 72、电路脉冲和网格扫描为 240 × 120、工业流线为 200 × 64、时间日期为 220 × 72、标题为 160 × 48、小卡片背景为 160 × 100、小图标背景为 64 × 64、科技面板为 260 × 180、纯文本和文字超链接为 160 × 48、图片为 160 × 100、轮播图为 240 × 160、按钮与全屏切换为 120 × 48、Switch 为 160 × 48、多选框和单选框为 200 × 96、下拉菜单为 180 × 64；圆形、雷达扫描和能量核心锁定 1:1，前端缩放和后端校验必须使用同一组约束。
- 图表静态数据每个组件最多 32 个数据点，雷达图至少 3 个维度，饼图和环形图只接受非负且总和大于 0 的数值；数据排名最多 12 项，实时告警和事件时间线最多 20 项，业务表格最多 8 列、20 行。以上限制必须在属性面板和 API 校验层同时执行，实时数据接入后仍只在节点中保存数据绑定 ID，不把连续数据流写入画布 JSON。
- 内置大屏模板是前端维护的静态预设，只负责实例化上述同一套白名单节点，不维护第二份持久化 schema。工作台 `#/templates` 只通过模板创建新草稿项目，禁止选择或静默覆盖已有项目；画布工具栏保留就近入口，只替换当前项目的本地编辑布局。两种入口都必须由交付人员显式保存，所有模板初始指标必须显示“示例数据”，不得伪装成已接入现场数据。
- 当前模板库至少覆盖设备检修、装备保障、工业制造、能源电力、园区运营、仓储物流、水务环保和数据中心。模板中心与宣传页的模板缩略图必须直接读取同一份 `CanvasNode[]` 布局数据生成，禁止另画一套与真实画布脱节的 CSS 占位图；新增行业入口时必须同时提供可创建、可保存、可继续编辑的完整 1920 × 1080 模板。
- 画布主题统一保存主题预设、背景纹理、字体风格、辉光强度、统一面板圆角，以及画布背景、组件背景、文字、强调和边框五个基础色。内置预设至少包含深海科技蓝、钢铁警示橙、能源监控绿、指挥中心金和明亮工业白；自定义主题仍只保存同一份正式配置。应用主题时必须显式更新所有非 3D 节点的颜色属性，新建非 3D 节点继承当前主题；不得改写 3D 节点的背景、灯光、材质或其他配置。主题与节点变更使用同一画布版本并由交付人员显式保存，禁止维护只存在于浏览器或预览态的第二份主题状态。
- 图表调整尺寸时必须按当前容器尺寸重新计算绘图区、标签密度和图元坐标，禁止整体非等比拉伸 SVG；文字点缀按容器重新排版，禁止通过 transform 拉伸字体；圆形和小图标背景必须锁定 1:1 宽高比。
- 组件内部只展示用户配置的内容、业务数据和必要的真实状态；编辑态、预览态及缩略图都不得叠加“可拖拽”“拖动”“点击配置”等操作提示、组件类型小字或资源数量。未绑定图片、轮播或模型时不在组件内显示导入引导，配置说明只放在组件库或属性面板。选中边框、缩放手柄和无障碍标签保留；加载失败、配置错误、失联/陈旧状态及可配置的示例数据标识不得被隐藏。
- 同一页面中的时间日期组件必须共享一个时钟调度器，不得为每个组件单独创建定时器；组件卸载且无订阅者后必须停止计时器。
- 新增 `card-title` 卡片标题和 `vector-icon` 矢量图标，最低尺寸分别为 120 × 32、24 × 24；独立图标锁定 1:1。标题支持简洁、细线、渐变色带、工业切角四种样式，文字与图标独立配置大小、颜色、粗细，拉伸容器不得改变字形。新节点校验和尺寸使用 `shared/canvas-ornaments.ts`，前后端复用；仅保存图标 ID 和轻量样式，不接受外部资源、任意 SVG、脚本或数据绑定。Lucide 0.468.0 的 36 个精选图标内置于 `shared/icon-catalog.ts`，许可证见 `licenses/lucide.txt`；严禁运行时访问图标 CDN 或外网字体。系统字体取本机可用字体，编辑器、预览与派生封面均支持新节点。
- 标题和图标保存依赖迁移 `0012_card_titles_and_icons.sql`，部署新代码前必须先执行迁移；迁移扩展节点白名单和尺寸下限，保留已有画布节点与配置。运行 `pnpm test:ornaments` 验证前后端校验、图标白名单、文字安全转义、缩放、主题和 SQLite 迁移/保存读取；测试使用临时编译目录和内存数据库，不修改项目数据（Node 22.13+）。
- 雷达扫描、数据流光、电路脉冲、能量核心、工业流线和网格扫描是纯视觉背景节点，只保存现有颜色、透明度等轻量配置，不接受外部资源或数据绑定。动画必须使用浏览器可合成的 CSS 变换/透明度并尊重 `prefers-reduced-motion`；编辑态、预览态与模板缩略图复用同一渲染，派生封面输出可识别的静态代表画面。保存依赖迁移 `0013_animated_decorations.sql`，封面渲染器版本为 5；`pnpm test:ornaments` 同时覆盖六种动画点缀的前后端约束、主题、缩放、封面和 SQLite 保存读取。
- 同一页面中的自动轮播图也必须共享一个低频时钟调度器；编辑态不自动轮播，最后一个订阅者卸载后必须停止计时器。链接、按钮和表单控件在编辑态不得触发跳转或业务交互，只能在预览/运行态操作。
- 全屏切换组件保存进入/退出文字、外观和禁用状态，最低尺寸 120 × 48，依赖迁移 `0014_fullscreen_toggle.sql`。编辑态只允许选中和配置；预览/运行态通过浏览器 Fullscreen API 让当前画布视口进入或退出全屏，并监听原生 `fullscreenchange` 同步全部同类组件状态。全屏缩放采用等比例铺满（cover），取消普通画布的 48px 留边和 1 倍缩放上限；不同屏幕比例下居中裁切溢出部分，禁止拉伸或保留黑边。浏览器权限关闭、目标容器缺失或请求失败必须显示错误并记录节点上下文，禁止静默失败。
- 画布元数据与组件节点分开持久化；图片、视频、模型和数据流不得内嵌进节点 JSON，只保存受控资源 ID 与数据绑定 ID。
- 当前图片资源只接受 PNG、JPEG 和 WebP，单张不超过 8 MB；服务端必须校验扩展名与文件签名，图片组件最多引用 1 个资源，轮播图最多引用 12 个同项目资源。
- 工作台 `#/resources` 是统一资源入口，按用户有权访问的项目切换范围，列出系统内置模型和该项目上传到对象存储的模型、图片、视频、音频。上传资源继续遵守项目编辑权限；视频只接受 MP4/WebM 且不超过 100 MB，音频只接受 MP3/WAV/OGG/M4A/AAC 且不超过 30 MB，服务端必须校验扩展名与文件签名。
- 系统内置资源在前端标记为不可删除，服务端也必须通过固定白名单拒绝删除，不能只依赖禁用按钮。用户资源删除前由服务端重新统计当前项目画布的资源引用；有引用但请求未携带显式确认时返回 `409 resource_in_use`，确认后才删除元数据和对象存储文件。删除不自动清空画布引用，失效引用必须保留用于定位；引用统计、删除结果和对象存储清理异常必须可观察。
- 资源预览必须继续经过项目访问控制：模型复用 `Model3DNode`（包括内嵌动画播放），图片、视频和音频使用受保护的资源内容接口。资源库媒体表依赖迁移 `0015_resource_library_media.sql`，实现或部署相关接口时运行 `pnpm test:resources`。
- 项目列表封面是已保存画布版本的派生 SVG，通过需要项目访问权限的封面接口按 `projectId + canvasRevision + COVER_RENDERER_VERSION` 校验缓存；封面渲染逻辑变化时必须更新渲染器版本，避免旧缓存保留已移除的展示内容。不得保存截图 Base64、复制整份节点 JSON 或占用模型对象存储。未保存过画布时必须明确显示无封面状态。
- 配置变更应生成版本；发布、回滚和当前生效版本必须可追溯。

## 模型与性能规则

- 内置公开样例使用 `shared/builtin-models.ts` 的固定 ID 白名单，当前为 `builtin:aqua-helix-hd-v1`。模型保存在 `apps/web/public/models/`，GLB 文件名含内容哈希；画布仍只保存 ID，API 在项目授权后认可该白名单。未知内置 ID 必须报错，禁止将任意 URL 当作内置模型。此静态分发仅适用于项目自带的公开演示资产；用户上传继续使用原有项目归属、权限、大小检查和 R2 流程。样例源文件和生成说明见 `demo-assets/aqua-helix-hd/README.md`。
- `model-3d.props.presentation` 由 `shared/model-presentation.ts` 统一校验，保存灯光模式、外壳模式、水流显隐和 0–1 拆解比例；旧数据缺失该字段时显式迁移为标准灯光/原始材质/水流显示/合拢。编辑器、2D 画布与运行预览复用 `Model3DNode`；检查面板使用 `ModelPresentationPanel`。不得将整套模型节点树或动画轨道写入配置。
- 检视能力由 GLB 节点 `extras` 标记：`label` 是显示名，`inspectionShell` 标记外壳，`category` 的 flow/animation 标记水流，`explodeOffset` 为有限三维本地位移。渲染器验证元数据并按实际能力启用控件；拆解时隐藏水流，合拢后恢复所选开关。原始节点名继续用于持久化绑定，中文显示名不得替代唯一节点标识。
- 切换模型资源时清除上一资源的节点外观与变换覆盖；材质修改必须按对象克隆，并在切换/卸载时释放克隆、纹理、几何和摄影棚后处理资源。摄影棚使用本地生成的环境反射；透明画布背景使用直接渲染以保留 alpha，不启用会覆盖透明度的辉光后处理。
- 内置模型变更运行 `pnpm test:models`；接口验收使用 `pnpm smoke:models`，只可对明确隔离、已迁移的本地测试 Worker 执行，具体命令见 README。
- 平台加载 GLB/GLTF 后默认循环播放文件内嵌动画，并允许在轻量画布配置中保存播放开关和速度；动画轨道仍属于模型资源本身，不复制进画布 JSON。编辑器与预览必须复用同一播放设置，整机自动旋转仍是独立场景配置，不得替代设备内部动画。
- 首期只接收 glTF 2.x 的 `.glb` 与 `.gltf`；单文件 `.gltf` 必须内嵌 buffer 与纹理，包含外部 URI 时必须明确拒绝并提示转为 `.glb`。保留原始文件、优化产物和可复现的导入检查报告。
- 画布节点只保存受控模型、图片、视频或音频资源 ID；Cloudflare 验证环境的文件通过 `PROJECT_FILES` 绑定进入 R2，客户现场通过对象存储适配层迁移到 MinIO/S3，不把文件二进制、Blob URL、对象存储公网 URL 或临时本地路径写入画布配置。
- 导入必须检查：文件体积、节点数、三角面数、材质数、纹理尺寸、缺失纹理、重复节点名、坐标单位和包围盒。
- 优化路径为几何压缩（Meshopt/Draco）、纹理压缩（KTX2/Basis）、删除无效资源、按车间/楼层/区域拆包；不要以降低渲染质量来隐藏资源问题。
- 资源应按需加载：首屏优先核心模型、状态和 KPI；非可见区域延迟或预加载。静态资源使用带内容哈希的缓存策略，实时数据和项目配置不可被错误缓存。
- 高频数据必须批量合并刷新，禁止每条消息直接触发一次 3D 渲染或 DOM 更新。
- 大量同类设备优先使用实例化、区域裁剪和 LOD。WebGL 或硬件能力不足时，展示可诊断的错误与设备信息，不伪装为正常运行。
- 同一 3D 场景中的多个模型实例必须共享一个 WebGL renderer、相机、灯光、控制器和动画循环；相同模型资源在单个场景内只加载和解析一次，实例克隆复用几何与原始材质。场景离开视口时暂停帧更新，资源结构变化或组件销毁时必须停止动画、释放派生材质/环境贴图/几何资源和 WebGL 上下文。不得退回“一个实例一个 canvas/renderer”的实现。
- 首期性能目标待首个真实项目压测后固化；在此之前不得把模型体积、首屏时间或帧率写成无依据的承诺。

## 开发与质量要求

- 当前技术栈：pnpm workspace；`apps/web` 为 React + Vite + TypeScript；`apps/api` 为 TypeScript Cloudflare Worker + D1。启动和验证命令以根目录 `README.md` 与各应用 `package.json` 为准。
- `apps/api/wrangler.jsonc` 绑定 D1 `factory-digital-twin-config`。业务表迁移只存放在 `apps/api/migrations/`，并且必须在迁移命令成功返回后才能让对应 API 依赖新表。
- 前端 API 地址只能从 `VITE_API_BASE_URL` 获取；本地默认值仅用于开发。生产域名、真实客户接口与密钥不可写入源码或提交 Git。
- P1 本地 REST 竖切的模拟设备由根目录 `pnpm dev:mock` 启动，只监听 `127.0.0.1:8790`。完整配置、状态切换和失联恢复步骤以 [本地 REST 设备联动测试](./docs/local-rest-device-runtime-test.md) 为准。
- 新增接口、配置 schema、导入文件格式或发布流程时，必须同步给出可验证的样例、校验规则和错误信息。
- 对数据接入、模型导入、发布流程等关键链路保留请求/任务 ID、项目 ID、版本 ID、耗时和失败原因，便于交付现场定位。
- 配置与运行态必须使用同一份被版本化的项目 schema；禁止分别维护会漂移的重复配置。
- 对外宣传页必须明确区分“交付场景示例”和有验收依据的真实客户案例；没有客户授权、项目证据与可核验结果时，不得使用客户名称、Logo、效果数字或暗示已经落地的文案。行业入口可以用于筛选场景与模板，但不能伪装成实际交付记录。
- 案例化模板允许使用虚构企业、城市、现场名称和符合行业语义的模拟指标增强项目感，但宣传页、模板中心和画布初始数据必须同时显示“虚构演示”或“模拟数据”；改为真实客户案例前，必须取得客户授权并补充可核验的交付证据。
- 依赖或服务端失败应暴露具体原因和上下文，不要添加静默兜底、宽泛 catch 或虚假的成功提示。
- 新功能至少覆盖：正常路径、输入校验失败、外部连接失败、数据陈旧/断线、权限失败和性能边界之一（按功能相关性选择）。
- 当前项目由用户单人开发，默认直接在 `main` 修改；无论改动规模或风险高低，Codex 都不得自行创建、切换或保留临时分支。只有用户明确提出创建分支时，才允许使用其他分支。
- 修改前仍必须检查工作区状态并保护用户未提交改动，禁止把无关文件混入修改或提交。

### 身份与权限基础

- 所有 `/api/v1/projects` 及后续配置、发布和数据源接口默认需要已验证的服务端会话；除健康检查、首次初始化和登录外，禁止匿名访问。
- 首个管理员只能在 `users` 表为空时，通过服务端 `BOOTSTRAP_TOKEN` 初始化。不得提供公开注册、默认用户名、默认密码或把初始化令牌下发进构建产物。
- 当前本地与 Cloudflare 验证环境仍是可清理的测试环境，密码长度临时按 6 至 256 个字符校验，前后端统一读取 `shared/auth-constraints.ts`；转入真实生产数据前必须恢复至少 12 位的密码强度要求并轮换全部测试凭据。不得为单个账号增加隐藏特判。
- Cloudflare Worker 的 Web Crypto 对 PBKDF2 最多支持 100,000 次迭代；当前新密码记录固定使用该上限，并在每条用户记录中保存实际迭代次数。切换运行时或密码算法时必须设计显式升级流程，不得直接提高默认值导致线上登录或初始化失败。
- 当前全局角色为 `platform_admin`、`delivery_manager`、`viewer`；项目成员角色为 `owner`、`editor`、`viewer`。权限判断只能在服务端执行，前端隐藏按钮不构成授权。
- 项目名称修改要求平台管理员或项目编辑权限；项目永久删除只允许平台管理员或项目负责人。删除必须清理项目关联配置，外部对象存储清理失败时必须记录上下文并向调用方返回明确警告，禁止伪装成完全成功。
- 用户、角色、会话和项目成员关系使用应用自有 schema 与 API 契约；Cloudflare Worker/D1 只是当前适配器。迁移到 Node/PostgreSQL 时必须保留这些接口与权限语义。
- 前端和 API 的认证交互默认同源，以 HttpOnly、`SameSite=Lax` Cookie 维持会话；本地开发通过 Vite `/api` 代理。跨站部署必须先完成 Cookie、CSRF 与反向代理设计，不能临时放开任意 CORS。

## 安全与交付

- 密钥、私钥、证书、真实客户模型、真实接口样本和生产数据不得提交。使用本地 `.env`、受控密钥存储和脱敏样本；新增环境变量必须写入 `.env.example` 与部署文档。
- 客户内网部署优先使用服务端数据网关/采集器与 Docker 化交付。网络拓扑、白名单、证书、数据访问权限和日志留存属于上线前验收项。
- 发布前检查至少包括：模型资源、资产映射、数据源连通性、字段/阈值、权限、性能结果和版本信息。任一阻断项失败不得发布。
- 当前临时云入口为 Cloudflare Worker；它只用于早期 API 验证，不能成为前端、数据模型或部署流程的隐性绑定。资源现状和迁移边界见 [Cloudflare-临时部署与迁移约定.md](./Cloudflare-临时部署与迁移约定.md)。
- Cloudflare 验证环境通过同一个 Worker 域名提供 Vite 静态前端与 `/api`，保持 HttpOnly Cookie 同源。账号未启用 R2 订阅时可以省略 `PROJECT_FILES` 绑定以发布其余能力，但健康检查必须标为 `degraded`，用户上传模型与图片的上传/读取必须分别明确返回 `503 model_storage_not_configured` 或 `503 image_storage_not_configured`；禁止改存 D1、静默丢弃文件或伪装上传成功。固定白名单内的公开样例随前端分发，不依赖 R2。

## 当前开发顺序

1. 完成 P0 身份、权限与项目基础：首个管理员、登录、会话、项目成员关系和项目列表。
2. 收集一份真实或脱敏 GLB/GLTF、资产台账、REST 和 WebSocket 样本，完成资产与数据标准确认。
3. 验证 P1 本地竖切：一个模拟 REST 设备通过 `assetId` 同时驱动 3D 状态、点击后的 2D 设备详情和中断失联提示。
4. 用真实或脱敏客户 REST 样本替换模拟源，再在目标浏览器、硬件和网络压测，固定数据网关及模型预算。
5. 再做 P2 配置台、模板、发布版本和部署流程；不在 P1 前扩展通用编辑器能力。

- 3D 组件 `showControlPanel` 为布尔值，旧配置缺省 `false`，前后端均须严格校验。2D 属性栏和 3D 编辑器均可配置并随画布保存。运行预览开启后复用 `ModelPresentationPanel`，提供外壳、动画播放/速度、水流、拆解和灯光控制，按模型能力禁用不支持项；预览操作只保存在当前组件的临时查看状态，禁止写回画布或设备。更换模型、保存预设变化、关闭面板或重新打开时恢复保存配置；支持收起和恢复预设。2D 编辑画布不显示交互面板。
